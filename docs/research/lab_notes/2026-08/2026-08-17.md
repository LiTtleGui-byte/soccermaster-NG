# 2026-08-17

## 解说生成链路因果瓶颈协议与存储恢复前准备

### 目的

把“数据时间配对、表征对齐、生成事实一致性”分开，建立从视觉/时间层到 Q-Former、projector、Llama 和解码的因果瓶颈定位协议。主要终点改为视频支持的事件、动作、结果和角色事实；reference overlap 降为辅助指标。

### 已确认

- 用户确认暂时固定现有 clip，不把 Temporal Pairing 纳入本轮排名。
- 允许使用模型评审，但 video-only fact、candidate-grounding 和 reference-support 使用分离的盲评任务。
- fixed-200 仅作开发；所有 probe 按比赛分组；最终需要来自新比赛且排除训练重叠的 Locked Match Holdout。
- 本地 `items.jsonl` 显示 3,256 条只覆盖 49 场；fixed-200 已覆盖其中 48 场。唯一未覆盖比赛为 Norwich 1–2 Chelsea，共 70 条，不足以承担最终 holdout。
- 已记录计划 `experiments/commentary_generation/CAUSAL_BOTTLENECK_EXPERIMENT_PLAN.md` 和 ADR `docs/adr/0001-video-grounded-commentary-diagnostics.md`。

### CPU-only grouped baseline

- 协议：`experiments/commentary_generation/MATCH_GROUPED_V3_DIAGNOSTIC_PROTOCOL_20260817.md`。
- 入口：`experiments/commentary_generation/event_separability_200_v3_match_grouped.py`。
- 首次启动因漏传脚本明确要求的 `PYTHONDONTWRITEBYTECODE=1`，在 preflight 立即退出；未读取缓存、未拟合 probe、未创建输出。随后只补齐该环境条件，fold、seed、标签和特征未改变。
- 正式运行退出码 0，耗时 4.134 秒；48 个 match groups 在五折中 train/test overlap 均为 0。
- mean-pooled macro-F1 从普通五折的 0.6169 修正为 0.6057，balanced accuracy 0.6040，打乱标签 macro-F1 0.1257。
- query-slot-preserving macro-F1 从 0.5657 修正为 0.5575，balanced accuracy 0.5698，打乱标签 macro-F1 0.0919。
- 结论：按比赛隔离后分数小幅下降但仍明显高于控制，支持 fixed-200 projected prefix 与 reference 事件存在关联；仍不支持视频事实、完整测试泛化或因果模块结论。
- 证据：`reports/commentary_event_separability_200_20260817_v3_match_grouped/`。

### 其他本地准备

- 数据角色清单：`reports/commentary_causal_bottleneck_preaccess_20260817/data_roles.json`。
- 评审与计分 schema：`experiments/commentary_generation/schemas/`。
- 配对、比赛聚类 bootstrap 指标入口：`experiments/commentary_generation/commentary_grounding_metrics.py`；合成自测通过。
- 逐层缓存与 oracle 静态契约已建立，`execution_authorized=false`。
- 全程未访问 NAS/GPFS 嵌入路径，未导入 Torch，未使用 GPU、模型、checkpoint、推理、生成或 SoccerMaster 训练。

### 风险与下一步

- 当前 3,256 条无法内部构造真正 match-isolated 的最终 holdout。
- 模型评审对未来生成错误的稳定性仍需 10% 重复顺序、正负控制和强模型复核。
- 本地 pre-access 统一静态验证已通过 16 项断言，记录为 `reports/commentary_causal_bottleneck_preaccess_20260817/validation.json`。
- 验证器第一次因环境没有 `python` 命令而未启动、未写结果；改用 `python3` 后通过。
- 真实最小逐层 cache 必须等待存储恢复、资产核对、`nvidia-smi` 和新的 GPU 授权。

### 临时存储绕行

- 基础设施反馈：200 节点的 `/remote-home` 因光交换机光模块故障预计中断数天；可以临时从 202 节点读取 `/remote-home` 并复制到 200 本地盘。
- 已建立 `STORAGE_BRIDGE_202_TO_200_20260817.md` 和机器可读搬运清单 `storage_bridge_manifest_202_to_200.json`；尚未确认 202 主机别名、200 目标目录或发起传输。
- 模型、checkpoint、tokenizer 和标注 JSON 可经 202 搬运；视频源实际位于 `/mnt/nas2`，仍需确认 202 是否能读 NAS，或请浩宁在 200 上只读挂载原 NAS。
- 搬运顺序锁定为 metadata → 完整模型资产 → 单个开发视频 → 平衡开发子集 → 冻结后的新比赛 holdout；禁止 `--delete`，所有大文件源端/目标端做 SHA-256。
- 随后 NAS 已恢复：原 train/test 根目录可读，fixed-200 首个样本可读且 `ffprobe` 通过（H.264、398×224、25 fps、750 帧、30.022 秒、4,896,229 bytes）。策略更新为视频直接从 NAS 只读使用，不再默认复制到 200 本地盘；202 绕行仅搬 `/remote-home` 模型与 metadata 资产。
- 系统映射确认当前主机为 gpu200 (`172.16.10.200`)，202 为 gpu202 (`172.16.10.202`)。gpu200 主动连接 gpu202:22 在 10 秒预检中超时，单次 ping 无回复；没有远端命令执行或数据传输。
- 已建立本地接收目录 `/home/tianlin/commentary_assets_202_bridge_20260817`。`/home/tianlin` 约剩 177 GB；更大的 `/raid` 当前为只读，因此未使用。下一步需要有效跳板/替代 SSH 入口，或从 gpu202 发起到 gpu200 的 dry-run 推送。
- 用户随后提供真实 SSH 路由：目标 `172.16.11.202:15654`，经 `bastion-DB13` (`202.120.39.165:58122`) 跳转。已写入 Linux 兼容的 `/home/tianlin/.ssh/config`（mode 600），省略 macOS `UseKeychain`。
- 跳板 SSH banner/key handshake 成功；观测到 ED25519 指纹 `SHA256:/p460eWah5vDQdtccbtOAanO24ix08XIPlM9MJVz96M`，尚未与 Mac trusted known_hosts 比对，因此未写本地 known_hosts。
- gpu200 上两把所需 identity 均不存在，当前也无 forwarded SSH agent；尚未尝试认证、远端命令或传输。
- 用户授权改用 gpu200 专用持久密钥。已生成 `/home/tianlin/.ssh/id_ed25519_gpu200_gpu202_bridge`（Ed25519、mode 600、无口令、指纹 `SHA256:T5sFuy2R8FOj4sG0DueQ9U4/4xlXb69fXWrDqOlMYcU`），并让 bastion/GPU202 两个 Host 共用该 identity。
- 首次远端授权无法由新密钥自举：gpu200 没有旧凭据。已在 `GPU200_GPU202_BRIDGE_PUBLIC_KEY_20260817.md` 给出跳板限定 `permitopen=172.16.11.202:15654` 和 GPU202 `restrict` 的两条 authorized_keys 记录，等待通过 Mac 现有访问各加入一次。
- 用户完成两端公钥授权后，gpu200 batch-mode 登录 bastion 与 GPU202 均通过；目标报告 `gpu202`/`tianlin`。
- 远端最小资产实测共 58,218,399,525 bytes（58.22 decimal GB；加 25% 余量 72.77 GB）。Phase-0 metadata 已复制到本地隔离目录，两份 JSON 可解析，test JSON 与 word-world 历史 SHA-256 一致；train JSON 新记录 SHA-256 为 `0c4580786a3e6db7bcd38040190b7f19b80fc28e51eac6307ae94354759ddfa8`。
- `/mnt/nas2` 在 Codex namespace 中显示 VFS `ro`、底层 SSHFS option `rw`；用户确认 NAS 实际物理剩余为十几至二十 TB，因此不采用 SSHFS 返回的 6.2 GB 作为容量依据。大模型传输暂缓，等待本地 staging 与 NAS 专用可写路径的目的地决定。
- 用户选定 `/mnt/nas/tianlin/SoccerMasterAssets`。在专用子目录完成 host 级 create/write/rename/delete 探针并清理探针后，将 58.22 GB 最小模型包从 GPU202 复制到 `commentary_generation_epoch11_20260817`。首次 `rsync -a` 因 SSHFS 拒绝 chgrp 返回 code 23，但文件内容已传输；改用 `rsync -rt --partial --no-owner --no-group --no-perms` 续传并以 code 0 完成。
- NAS 最终清点为 117 files / 58,218,293,029 bytes。视觉 backbone SHA-256 为 `fc64d2acbabb5c20a3e0bf996906954c81838d8495b7724a9862199c0af4c977`，生成 checkpoint SHA-256 为 `e1ff7fef61a480576d52f4c2761ccedca16d8af3ccd6cdc39a83d36fc5a32317`，均与 GPU202 源端一致；Llama/BERT/SigLIP2 的 checksum dry-run 均为零待传文件。
- 新增显式资源根 `SOCCERMASTER_COMMENTARY_ASSET_ROOT`，不设置时继续使用历史 `/remote-home` 路径，设置时解析 NAS bundle v1。纯 CPU 预检通过：train/test 为 24,027/3,256 条，epoch-11 checkpoint 可用 CPU mmap 打开，953 个 state keys 及各组件前缀完整。结果为 `reports/commentary_nas_bundle_preflight_20260817/result.json`；该检查未查询 GPU、未执行 forward/generate/training。
- 冻结资源清单 `NAS_COMMENTARY_BUNDLE_MANIFEST_20260817.json` 已复制到 NAS bundle 的 `manifests/`，本地与 NAS SHA-256 均为 `1d56f2e0b8e7602dc54d7d3266b5cc7db0668e00a806b10fe01a692516370bac`。
- 宿主机 `nvidia-smi` 于 08:06:37 成功：8×H800 均由 PID 2637014 (`...g/VLA/openpi/.venv/bin/python`) 占用约 36.1–36.3 GiB，利用率 98–99%。因此不启动解说模型。资源快照见 `reports/commentary_gpu_preflight_20260817/README.md`；任何后续 GPU 操作仍需 fresh `nvidia-smi` 和精确命令授权。
- 已按用户需求准备 GPU200 的条件式自动排队器，但未授权、未启动。它每 30 分钟查询一次，空闲定义为无 compute process、利用率不高于 5%、空闲显存至少 60,000 MiB，并要求间隔 60 秒两次确认；先运行单样本 inference-only smoke，随后仅在 8 卡全部空闲时运行 8×407 条逐层缓存和基线生成，CPU 合并后强制停在人工审核闸门。配置为 `GPU_AUTORUN_CONFIG_20260817.json`，instructions 为 `GPU_AUTORUN_INSTRUCTIONS_20260817.md`。当前 `execution_authorized=false`，arm 拒绝自测通过；没有查询或使用 GPU。
- 用户随后明确批准该固定自动范围。`AGENTS.md` 已记录一次性窄范围例外，配置切换为授权并以 SHA-256 `67108f076d152422fa69fb150b8bf06f37d100d273e3904cefe0271290b72566` arm；tmux `commentary-gpu-autowatch` 已启动，pane PID 2690664、状态存活。首次快照中 GPU0–7 利用率为 96–99%，均由 PID 2637014 占用，监督器正确保持 `waiting_for_one_gpu`，未触发 smoke；下一次轮询间隔 1,800 秒。
- 后续快照一度显示 GPU0–7 利用率 0%、每张空闲约 80.5 GiB，但均保留 PID 3375814 的约 528 MiB CUDA context。用户明确批准只在 GPU1 空闲显存至少 30,000 MiB、利用率不超过 5% 时与该 PID 共存运行一次 smoke；八卡阶段仍要求无其他 compute process、每张至少 60,000 MiB。旧等待监督器安全中止且无 GPU worker/输出，旧 arm 保留；修订配置以 SHA-256 `0439fe41b81cb5b91f95740cfcaf19641ae48a104772490466aaf693e7c75199` 重新 arm，并在同名 tmux 重启。重启后的即时快照中 PID 3375814 已恢复计算，GPU1 利用率 99%，因此监督器正确拒绝 smoke 并继续等待。

## G10-B 255帧Refiner probe CPU-only静态预检

- 目的：在不使用GPU、不导入Torch/Refiner且不加载checkpoint的条件下，固定SNGS-10004单样本Refiner probe的输入、源码、配置、checkpoint和未来副作用边界。
- gpu200的 `/remote-home` 未挂载；根据用户提供的共享GPFS路径，通过 `GPU202-tianlin` 只读确认Refiner提交、4个源码/配置文件和323,985,486-byte checkpoint身份。checkpoint SHA256为 `d8e383e93e209e4ac3aa8fb97d8b209fe10b183f622cbae40d9124160238fe5a`；未反序列化。
- 新增两份隔离配置、manifest和受保护launcher。baseline与target逐叶差异仅为 `data.max_frames`，由上游base解析的750显式改为255；输入仍是enrichment run2固定归档，输出和临时pickle使用全新workspace-only命名空间。
- 正式预检退出码0，结果为 `reports/g10/20260817_refiner_probe_run1_preflight/result.json`。上游删除同名ZIP、CWD临时pickle、模型加载和device选择副作用已登记；未来运行固定单卡批准变量、30秒心跳、14,400秒timeout、无fallback/重跑，并禁用评估、可视化和训练。
- 已确认：静态配置/资产/输入身份与隔离运行合同就绪。未知：checkpoint能否实际加载、255帧forward及输出是否正确。风险：gpu200仍需恢复 `/remote-home` 挂载或另行批准资产迁移；实际运行前仍需fresh `nvidia-smi`和新的精确GPU授权。
- 本轮未执行 `nvidia-smi`、GPU、Torch/Refiner导入、checkpoint反序列化、model load、forward、Step 3、转换、评估、可视化或训练；按用户要求停止。

## gpu200 `/remote-home` 挂载恢复复核

- 同日后续只读检查确认当前主机为 `gpu200`，`/remote-home` 已挂载为来源 `gpfsdata`的 GPFS，挂载选项包含 `ro`。
- `Soccer-Backbone`、Refiner、共享参考 Python 均可访问；Refiner `best_model.pth` 可见且大小为 323,985,486 bytes，与冻结 manifest 记录一致。本次没有重算大文件哈希或反序列化 checkpoint。
- 上文“当时未挂载”仍是静态预检执行时的历史事实；当前“等待挂载恢复”阻塞已解除。Refiner实际运行仍需fresh `nvidia-smi`、资源/进程报告和针对单次命令的新GPU授权；本轮未执行GPU查询或运行。
