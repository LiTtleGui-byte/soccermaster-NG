# 2026-08-20 UTC

## 本地代码与 checkpoint 兼容分层

- **已确认**：既有本地资产约25 GiB，已经具备基本物理分层；本轮没有移动或复制大型文件。
- **修改**：新增`configs/assets/local_assets.yaml`、checkpoint分类软链接、`src/soccerfactory_adapters/`和`docs/LOCAL_PROJECT_LAYOUT.md`。历史Gate manifest、报告和原始远端资产均未改写。
- **结果**：19个工作区内规范路径全部可解析；新adapter读取现有Step-3归档后得到255帧、3,390人物和255帧有效相机参数。
- **边界**：这是代码与资产组织工作，不是新的Gate、GPU实验或语义质量验证。

## 项目目录收敛

- **已确认**：冻结原版、研究代码、资产、运行结果和历史证据已分别进入`baseline/`、`research/`、`assets/`、`runs/`和`archive/`。
- **异常与修复**：首次移动因测试目标目录漏建而立即停止，补建后从精确位置继续；首次CPU导入发现`sn_calibration`的顶层模块路径依赖，随后通过研究源码链接和启动器显式路径解决。
- **复核**：核心代码/配置CPU导入通过，根`train.py --help`与`eval.py --help`均退出0。没有GPU、forward、评估或训练。
- **风险**：历史脚本保留运行当时的绝对路径，重新执行前必须查看`archive/PATH_MAP.md`并按当前安全规则重新审查。

## 目录迁移后的 SoccerMaster 五头路径 smoke

- **已确认**：用户查看资源报告后批准物理GPU 7执行一次固定30帧、五任务头、inference-only smoke；退出码0，五头和5张图齐全。
- **输入与路径**：可修改源码来自`research/src`，配置来自`research/configs/baseline`，checkpoint来自`assets/checkpoints/official/soccermaster/epoch_19`，固定数据视图最终解析到`assets/`图片和`.runtime/`训练PKL。
- **资源与结果**：脚本内77.969秒，峰值GPU allocated/reserved为6,422,995,456/8,529,117,184 bytes；日志和机器结果位于`runs/path_smoke_20260819/soccermaster/`。
- **边界**：没有训练、backward、optimizer、scheduler或fallback；这是目录迁移后的路径诊断，不重新判定G4，也不证明总体语义质量。

## 目录迁移后的 SoccerFactory 全链路径 smoke

- **已确认**：用户查看fresh GPU快照后批准物理GPU 7执行固定SNGS-10004全255帧路径smoke；Step 1、enrichment、coord-only Refiner、CPU Step 3、训练PKL转换和真实DataLoader全部退出0。
- **路径**：算法代码来自`baseline/code/soccerfactory`，权重来自`assets/checkpoints/official/soccerfactory`，图片来自`assets/data`，格式转换使用`research/src/soccermaster/integrations/soccerfactory`。
- **结果**：各阶段保持255帧、3,390人物和48轨迹；enrichment生成3,390条坐标/角色/球队、1,999条非空号码和255帧相机参数；最终DataLoader batch为`[1,30,3,512,512]`。最高GPU reserved为19,639,828,480 bytes。
- **证据与边界**：结果位于`runs/path_smoke_20260819/soccerfactory/`；无训练、评估、fallback或自动重跑。该结果只证明路径和接口，不证明标签语义准确，也不改变G10状态。
