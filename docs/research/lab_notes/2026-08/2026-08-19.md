# 2026-08-19

## SoccerFactory 球队颜色第二场固定诊断

- 目的：检查`SNGS-10004`上显著优于ReID的球衣颜色分组能否在一场预先固定的新比赛上保持。
- 非目标：不修改生产链、不写回标签、不运行模型/GPU，也不在人工复核前报告跨比赛准确率。
- 结果：`reports/g10/20260818_team_color_cross_match_sngs10001/result.json`；联系图和人工标注页位于同目录。

已确认：本地运行缓存只有`SNGS-10004`。只读历史Step-3归档包含1000个成对视频成员；在查看结果前固定选择“排除10004后编号最小且成员、字段和源JPEG完整的序列”，得到`SNGS-10001`。该场包含750帧、11,494条检测和40条轨迹，未按结果重新选样本。

已确认：历史ReID球队簇覆盖29/40条轨迹，cluster 0/1为14/15，11条缺失；全部轨迹颜色KMeans为24/16，silhouette 0.506。视觉复核显示蓝色与酒红色主要外场球衣形成清楚分组，但混有足球/局部人物误检、门将和裁判，尚不能代替人工准确率。

已确认：第一场的固定角色/长度过滤规则在第二场选出0/40条轨迹，因为每条满足长度条件的轨迹都至少出现过一次非player `role_detection`信号。脚本按预先协议报告`not_fit_insufficient_eligible_tracks`，没有放宽阈值或重选比赛。这表明“任何一次非player信号即排除”不具备跨场稳健性。

已准备：40条轨迹的浏览器标注页显示三个跨时间裁剪，并用洋红框标明目标；40个本地裁剪和40个页面引用均已核对。人工标签不会直接写回项目或历史归档。

已确认：用户完成40条轨迹复核，其中34条为球队明确的外场球员（蓝14、酒红20）、2条门将、2条裁判/工作人员、2条非人物/无法判断。机器结果为`reports/g10/20260818_team_color_cross_match_sngs10001/annotation_evaluation.json`。

已确认：多数类基线为20/34（58.8%）。历史ReID覆盖29/34（85.3%），覆盖内28/29正确（96.6%），把5条缺失输出计错后的端到端结果为28/34（82.4%，balanced accuracy 83.9%）；唯一覆盖内错误为酒红轨迹17，缺失的外场轨迹为8、15、19、22、54。颜色方案覆盖34/34，33/34正确（97.1%，balanced accuracy 96.4%），唯一错误为目标极小且模糊的蓝队轨迹15；预先声明的敏感性结果排除15后为33/33。

已确认：历史最终角色在38条角色明确轨迹上检出全部4条门将/裁判，但同时把5条外场球员误归为非player，非player precision 44.4%、recall 100%；这5条正是ReID球队簇的外场覆盖缺口。逐检测“只要出现过一次非player即排除”的规则更差，固定第二场0/40可用。

推断：颜色特征在两场开发诊断中表现稳定，适合作为外场球队归属的主要候选；ReID不应被描述为每场都不准，第二场覆盖内准确率很高，但其第一场发生聚类塌缩、第二场受硬角色门控造成覆盖缺口，整体行为不稳定。角色信号应改为软置信度或轨迹级多数聚合，不能作为一次命中即排除的硬门。

描述性汇总：两场来源谱系不同，共73条球队明确外场轨迹；颜色覆盖100%、71/73正确（97.3%），ReID覆盖68/73（93.2%）、覆盖内51/68正确（75.0%）、端到端51/73正确（69.9%）。该汇总不是正式benchmark或生产就绪结论。

下一步：实现一个只读取固定归档、不覆盖现有标签的离线球队候选器：外场轨迹使用跨帧上半身颜色聚类，角色只作为软证据，门将/裁判/低可见性轨迹输出unknown；先在这两场回放并确认输出契约，再考虑接入生产链。

## 离线球队候选器首次双场回放

- 目的：冻结并回放双层输出契约——所有轨迹保留`raw_color_cluster`，只有满足角色/可见性/颜色间隔规则的轨迹得到`auto_team_cluster`。
- 非目标：不接入或覆盖生产标签，不使用人工标签生成候选，不运行GPU/模型/生产链。
- 入口：`experiments/soccerfactory_visualization/replay_team_assignment_candidate.py`；报告目录为`reports/g10/20260819_team_assignment_candidate_replay/`。

已准备：候选规则在运行前固定为显式门将/裁判unknown、少于3个有效裁剪unknown、持续非player证据进入复核、颜色间隔低于0.15进入复核；人工标签只允许在候选JSON写出后读取。代码还要求新输出与既有两场颜色诊断在簇标签置换意义下100%一致。

已确认：首次双场单进程CPU回放在300秒timeout触发退出码124，没有Python异常输出，进程已结束且无残留。候选输出采用两场全部完成后才写出的事务式设计，因此`candidate_outputs.json`和`evaluation.json`均未生成，不能形成通过或指标结论。没有使用GPU、没有修改源归档、没有自动重跑。

推断：此前750帧第二场单独特征提取已接近较长timeout，本次最可能受远端JPEG随机读取耗时影响；由于脚本尚无分场计时日志，精确卡点未知。

下一步：把提取阶段改成每场独立、原子写入本地特征缓存并分别设置timeout；两场缓存都成功后再运行无JPEG读取的候选合并和人工后验评价。首次失败后不在本轮自动执行该fallback。

### 已批准的分场缓存fallback

已确认：用户明确批准分场缓存fallback。`SNGS-10004`在10.2秒内生成49×26特征，`SNGS-10001`在约27秒内生成40×26特征；远端文件缓存变热可能解释第二场较此前明显加速，但未单独测量，不能外推稳定性能。两份NPZ和轨迹JSON均通过临时文件写完后原子改名，元数据记录提取阶段未读取人工标签。合并阶段只读本地缓存，候选JSON先写出，随后才读取人工标签评价；退出码均为0。

已确认：两场候选颜色簇与既有诊断在标签置换意义下100%一致。73条球队明确外场轨迹的原始颜色层覆盖100%、71/73正确（97.3%）。固定安全接受层覆盖60/73（82.2%），接受部分60/60正确；第一场颜色错误轨迹20被低颜色间隔拦截，第二场错误轨迹15被持续非player证据拦截。

已确认：安全角色目标未达成。12条人工确认门将/裁判只拦截5条（41.7%）；第二场4/4全部拦截，第一场仅1/8。第一场最终角色全部为player，使7条门将/裁判仍得到自动球队簇。候选执行和输出契约通过，但自动接受层不能视为生产安全。

推断：上半身颜色作为外场球队分组候选已经获得两场一致支持；角色/非球员判断是独立瓶颈，必须与球队颜色模块解耦。进一步压低颜色错误可以通过拒绝换取精度，但在角色信号未修复前，拒绝策略无法保证门将/裁判为unknown。

下一步：针对这12条非球员和73条外场轨迹，设计一个与球队颜色簇独立的轨迹级角色候选（优先使用角色时序分布、球衣颜色离群度和球场位置），先离线评价非球员召回与外场误拒率；角色门控通过前不接入生产标签。

## 轨迹级角色固定规则候选

- 目的：用不训练模型的固定规则组合角色时序、颜色离群和球场位置，评价能否安全拦截门将/裁判。
- 非目标：不读取人工标签生成候选，不调球队颜色模块，不使用GPU或训练，不写回标签。
- 结果：`reports/g10/20260819_track_role_candidate/evaluation.json`；逐轨迹候选为同目录`candidate_outputs.json`。

已确认：两场85条角色明确轨迹包含73条外场和12条门将/裁判。严格`nonplayer`只命中2/12，precision 100%、recall 16.7%；两条均来自第二场明确最终角色，第一场严格召回0/8。把所有nonplayer/review/low-evidence共同视为安全拦截后命中11/12，precision 40.7%、recall 91.7%，唯一漏掉第一场裁判17。

已确认：扩大拦截同时误拒16/73条外场，误拒率21.9%，超过脚本预先固定的15%边界；角色明确轨迹自动放行率仅68.2%。第一场拦截7/8非球员、误拒7/39外场；第二场拦截4/4非球员、误拒9/34外场。

已确认：16条外场误拒中，10条来自少于3个有效裁剪，4条来自颜色簇最异常10%，2条来自持续`other`信号。召回提升主要依赖扩大复核面，并非角色分类本身可靠。规则判定为`role_candidate_not_sufficient_for_production_gate`，没有基于结果调阈值或自动重跑。

推断：第一场稀疏/错误的角色输出无法由简单轨迹多数、颜色离群和球门位置可靠补救。颜色离群同时会抓住遮挡或困难外场轨迹，短轨迹也不应自动等同非球员。继续用这两场调启发式会过拟合。

下一步：只读审计`role_confidence`的具体语义、shape与轨迹时序分布，判断能否进行置信度加权聚合；若置信度本身不携带可分信息，则停止启发式角色门控，转向新增独立角色监督/模型，而不是继续调阈值。

## `role_confidence`语义与聚合审计

- 目的：确认两场归档中`role_confidence`的生产者、数值含义和轨迹聚合用途，并判断只改置信度加权是否可能补救角色门控。
- 非目标：不运行GPU/模型或训练，不选择置信度阈值，不修改源归档、人工标签或生产链。
- 入口：`experiments/soccerfactory_visualization/audit_role_confidence.py`；机器结果和说明位于`reports/g10/20260819_role_confidence_audit/`。CPU运行退出码0。

已确认：SNGS-10004配置使用PRTReID。逐帧角色取`role_cls_scores['globl']`的argmax，`role_confidence`保存同一原始分数向量的最大值，没有softmax或校准；3,176个值中位数3.700、范围0.402–3.936。SNGS-10001归档配置明确使用Qwen2.5-VL角色模块；代码对成功解析的角色统一写1.0，该场11,494行全部恰好为1.0。因此同名字段在两套模块中分别表示原始最大logit和硬标签有效性指示，不能当成统一概率。

已确认：现有`select_highest_voted_att`按角色累加该字段，累计值最大者成为整条轨迹角色。在两场全部89条轨迹上，加权投票与普通按帧计数投票0条不同，加权结果89/89复现保存的最终角色；只改投票权重没有修正任何轨迹。

已确认：85条角色明确轨迹含12条门将/裁判。严格以最终`goalkeeper/referee`拦截时命中2/12、precision 100%、recall 16.7%；把`other`也当非外场可命中4/12，但误拒5条外场，precision 44.4%。PRTReID场次8条人工非外场全部聚合为player，其中6条逐帧top-1从未出现任何非player角色，无法由现有归档后处理恢复。

已确认：归档只保留top-1角色及一个最大值，不保留完整类别分数、第一二名margin或熵，因而无法补算softmax或做可靠的多分类校准。Qwen场次`goalkeeper/referee`帧比例的描述性AUC为1.0，但这完全来自标签频率而非恒为1的`role_confidence`；PRTReID场次相应AUC仅0.615。该结果仅来自两个开发场次，不构成跨比赛指标。

推断：当前问题同时包含字段契约缺陷和模型行为差异。统一调`role_confidence`阈值既混淆生产者语义，也无法补回PRTReID未保留的角色证据；继续在相同两场调启发式会过拟合。

下一步：停止在现有两场调角色置信度启发式。未来一次角色推理先按生产者专属schema保存完整逐帧角色分数或显式abstention分数，再在新的人工标注比赛上评价固定轨迹聚合；在此之前不把角色门控接入球队自动赋值。

## SNGS-10004同场Qwen角色替换

- 假设：固定第一场人物框、轨迹和取帧，只把PRTReID角色模块换成第二场使用的Qwen2.5-VL，可以判断PRTReID是否构成角色失败的因果瓶颈。
- 实现：在不读取人工标签时冻结49条轨迹、137个跨时间代表视图；Qwen同时读取全帧与精确人物裁剪，保存原始生成标签和四个候选字符串的序列似然。轨迹候选先写出，随后才读取既有人工标签。
- 运行：用户查看fresh GPU状态后明确批准物理GPU 1一次运行。Qwen2.5-VL-7B以BF16、batch 4完成137/137视图，退出码0、总耗时52.1秒、峰值allocated/reserved约16.2/18.2 GiB；无训练、checkpoint、fallback或自动重跑。日志和结果位于`reports/g10/20260819_qwen_role_swap_sngs10004/`。

已确认：把明确生成的`goalkeeper/referee`作为非外场证据时，Qwen命中3/8条人工门将/裁判，precision 100%、recall 37.5%，外场误拒0/39；命中裁判4、17和门将12。相同比赛的PRTReID基线为0/8，因此角色模块替换带来真实改善，但仍漏5/8。

已确认：把Qwen的`other`也算非外场可覆盖8/8，但同时误拒25/39条外场，precision仅24.2%、外场误拒率64.1%。137个视图中82个输出`other`，说明它主要是宽泛的不确定/不匹配类别，不能等同门将或裁判。基于四候选序列似然的轨迹聚合结论相同，没有解决该混淆。

推断：PRTReID是第一场角色失败的一部分瓶颈；Qwen明确角色可作为高精度补充信号。但当前单帧裁剪对小目标、模糊和倒地门将信息不足，Qwen常退化为`other`。继续调`other`阈值只会在召回和大量外场误拒之间交换。

下一步：用同一批冻结视图构造“一条轨迹的多个视图一次输入”的Qwen轨迹级提示，只比较其明确角色召回与外场误拒；不重新抽帧、不调标签阈值。该实验属于新的GPU范围，尚未授权或运行。

### Qwen轨迹级多视图后续实验

已确认：用户指定物理GPU 1并在看到该卡已有PID 1444761约4.5 GiB占用、实时约57% SM后确认继续共存。实验固定此前49条轨迹和137个视图，每条轨迹把最多3个时间点的整帧与人物裁剪一次性交给同一Qwen提示；BF16完成49/49，退出码0、耗时25.7秒、峰值allocated/reserved约15.9/16.7 GiB，无训练、checkpoint、OOM、fallback或重跑。

已确认：多视图明确`goalkeeper/referee`只命中2/8，precision 100%、recall 25%，外场误拒0/39，低于单帧投票的3/8。它识别门将12、42，但单帧已经识别的裁判4、17被重新输出为player。候选序列似然argmax与生成标签结果一致；把other纳入非外场也没有新增命中，因为多视图输出只有47个player和2个goalkeeper。

推断：多张静态图本身没有让当前通用Qwen形成更好的轨迹角色判断，且提示中的整体判断倾向把困难目标归为player。该实验正式判定`track_multiview_does_not_improve_single_view_baseline`，不保留当前多视图提示，不继续在同一开发比赛调提示词。

下一步：角色模块方向暂停在“单帧Qwen明确角色可作高精度补充，但召回不足”的结论；若继续，应转向专门角色模型或新的独立比赛验证，而不是在现有两场继续调prompt/阈值。

## SNGS-10004 Refiner coord-only run2

- 目的：验证固定255帧enrichment归档能否通过保存版Refiner的coord-only checkpoint完成一次真实单样本forward并产出结构合规归档。
- 非目标：不评价坐标语义质量，不运行可视化、Step 3、转换或训练，不自动重跑。
- 运行：用户查看fresh资源报告后明确批准物理GPU 7。2026-08-19 05:08 UTC在新tmux会话 `g10_refiner_probe_gpu7_run2`、窗口 `refiner_run2` 启动；GPU 7启动时已用0 MiB、空闲81,090 MiB、利用率0%且无compute process。GPU 0的其他用户进程保持不变。

已确认：Refiner revision为 `d4a06f77aebcb45eea1e54b47991dc80ee0f239a`，模型为`SoccerTrackerTransformerTimeSformer`，只启用`coord`任务。固定255帧按三个100帧clip完成checkpoint加载和forward；退出码0，总墙钟7.009秒，视频处理/推理日志约1.30秒，无fallback或重跑。

已确认：输出为 `.runtime/g10/sngs10004_refiner_probe/run2/output/refined_sn-gamestate.pklz`，38,952,351 bytes，SHA256 `76eec06718b9f5c0e24e4d881a80af8be0b942c20f97f5d9d63b29f5c20a6041`，成员严格为`10004.pkl`和`10004_image.pkl`。机器结果和日志位于`reports/g10/20260818_refiner_probe_run2/`。进程结束后GPU 7已释放。

未知：尚未比较Refiner前后的坐标误差、轨迹抖动或视觉合理性；单场结构通过不能外推其他比赛，也不表示G10-B整体通过。

下一步：CPU-only读取前后归档，量化`bbox_pitch`位移、边界、非有限值和轨迹时间连续性，并生成最小可视对照；完成后再判断是否有依据推进Step 3。

## 48条attribute probe完成并进入stage3_ready

已确认：heartbeat在05:08:40 UTC发现GPU 1–7空闲，连续两次确认后按固定规则选择物理GPU 1。冻结backbone特征抽取完成48/48条，退出码0、耗时115.81秒、峰值allocated约3.33 GB；随后CPU match-grouped probe退出码0，17项任务完成，最佳macro ROC-AUC为0.77758。产物位于`reports/commentary_attribute_probe_pilot_48_20260818/`。

已确认：固定readiness条件全部通过。既有逐接口证据中Q-Former下降-0.03219，约为时间阶段下降-0.01090的2.95倍；projector变化+0.00057。supervisor在05:12:10 UTC写入`stage3_ready`并停止，一次性授权已消耗；没有执行阶段3、主模型训练或checkpoint写入。

推断：当前48条/36场开发证据把Q-Former提升为首要因果干预候选，但不能证明它是唯一瓶颈或代表总体性能。

下一步：实现并CPU-only预检interface-matched Q-Former oracle steering launcher、固定manifest、输出契约与公平性断言；GPU执行仍属于新的运行范围。

## Refiner前后坐标效果审计

- 目的：在没有二维真值的前提下，比较固定SNGS-10004在Refiner前后的坐标改变量、球场边界和时间连续性。
- 非目标：不把平滑等同准确，不使用GPU、模型、Step 3或训练。
- 入口与产物：`experiments/soccerfactory_visualization/audit_refiner_coordinate_effect.py`；结果与图位于`reports/g10/20260819_refiner_coordinate_effect/`。

已确认：首次运行因Matplotlib版本不支持`tick_labels`在绘图阶段退出码1，未生成正式结果；日志保留。两处改为兼容的`labels`后一次针对性重跑退出码0、墙钟1.70秒、峰值RSS312,024 KiB。

已确认：3,176/3,176个坐标发生变化，位移中位数0.204米、95分位0.511米、最大1.091米；没有新增越界。相邻帧位移中位数由0.154增至0.253米（比例1.643），二阶差分中位数由0.213增至0.401米（比例1.883）。49条轨迹中分别有40条和35条在两个时间连续性指标上增大。

推断：Refiner在本样本上没有呈现预期的平滑行为，且不是少数异常值造成；这构成继续SoccerFactory Step 3前的质量风险。由于没有二维坐标真值，尚不能断言绝对几何精度变差。

下一步：CPU-only可视化高位移轨迹和100/200帧clip边界附近的前后轨迹，区分普遍逐帧噪声与分clip/padding伪影；完成前不推进SoccerFactory Step 3。

## SNGS-10002新比赛Qwen盲推理与Q-Former阶段3启动

已确认：用户在fresh GPU0–7全部空闲的资源报告后，确认以两个独立tmux并行启动。SNGS-10002被固定为未参与前两场角色诊断的新比赛；33 tracks按同一最多三视图规则得到89个label-blind输入，GPU manifest不含历史角色字段。

已确认：新比赛Qwen在物理GPU 1完成89/89视图，退出码0、耗时27.95秒，峰值allocated/reserved约17.41/20.59 GB，无OOM、训练、fallback或重跑。盲输出为other 62、player 21、referee 5、goalkeeper 1；独立标签尚未读取或创建，所以当前不能报告精度、召回或外场误拒。产物位于`reports/g10/20260819_qwen_role_newmatch_sngs10002/`。

已确认：Q-Former阶段3最终配置SHA-256为`3627d58ce1cce5d731f9730ff243a9514991671742a9a53bce031481d691c545`。CPU准备完成48条四条件干预张量，连续两次空闲确认后于05:32:58 UTC在物理GPU 0进入`running_stage3_generation`；tmux为`commentary_qformer_stage3_gpu0:stage3`。尚无完成或效果结论。

下一步：等待阶段3按固定协议停在`review_required`或失败；新比赛Qwen则冻结独立人工角色标签后再评价，禁止使用历史Qwen伪标签自评。

补充只读分析：新比赛Qwen的33条轨迹中16条存在跨视图输出冲突；多数输出为other 27、player 4、referee 2。明确非外场视图分布在轨迹10/13/14/22/23（referee）和27（goalkeeper）。目视抽查轨迹27的三帧更像同一蓝队外场球员，但Qwen给出两次player和一次goalkeeper；轨迹22的单帧黑衣目标更符合referee。该抽查不是正式标签，但说明输出自一致性较弱。结合用户指出的潜在ID switch，正式页面需增加身份一致性和逐视图角色，不应强迫所有轨迹只有一个角色标签。

### Refiner clip边界归因

- 入口：`experiments/soccerfactory_visualization/diagnose_refiner_clip_boundaries.py`；结果和逐帧图位于`reports/g10/20260819_refiner_coordinate_quality/`。
- 非目标：不重新运行Refiner、不使用GPU、不把时间平滑代理成二维坐标准确率、不启动Step 3。

已确认：保存版推理把255帧无重叠地切为0–99、100–199、200–254三个clip，输出直接拼接；模型使用归一化坐标上的residual回归。跨100/200边界的27个连续步，中位步长由0.169米增至0.615米，中位增量+0.354米，边界确实放大跳变。

已确认：边界不是整体退化的主要解释。段内2,933个连续步中位数仍由0.154米增至0.251米，中位增量+0.075米；去除边界邻域后，2,768个二阶差分中位数仍由0.212米增至0.397米。边界邻域只占全部二阶差分样本的1.84%。逐帧图也显示大多数时段Refiner曲线高于输入，而不是只在100/200帧出现尖峰。

推断：增加overlap/blending可能缓解两个局部断点，但无法修复普遍的段内residual抖动。由于SNGS-10004的sn500目录只有图像、没有可用二维人物坐标真值，本轮仍不能判断绝对几何误差变好还是变差。

下一步：若目标是先证明SoccerFactory全链可运行，可把当前结构合规Refiner归档用于一次固定Step 3接口smoke，但必须保留“坐标质量未验证且时间平滑变差”的限制；若目标是判断是否采用Refiner，则应改用带二维球场坐标真值的独立序列做前后误差比较。

## Q-Former阶段3生成完成，等待盲审

已确认：物理GPU 0上的48条×4条件inference-only生成全部完成，worker退出码0，生成阶段耗时331.829秒，峰值allocated/reserved约15.16/15.25 GiB；checkpoint只加载一次，没有视觉或Q-Former forward、训练、backward、optimizer、scheduler、checkpoint写入、fallback或自动重跑。supervisor已进入`review_required`并释放GPU。

已确认：相对cached baseline，norm-matched错配方向control、oracle residual alpha 0.5和alpha 1.0分别改变23、24和34条token序列。结果位于`reports/commentary_qformer_oracle_steering_pilot_48_20260818/result.json`，盲化配对包为同目录`paired_review_packet.json`。

未知：尚未按视频事实完成盲审，因此输出变化不能写成事实改善，也不能据此确认Q-Former是因果瓶颈。

下一步：CPU/人工盲审四条件，按预注册的中心事实改善、退化和错配control标准形成结论；盲审前不追加GPU运行。

## G9六卡三步观察运行

- 目的：让G9真实全主干五头训练在可控范围内跑起来，观察数据、forward/backward、DDP、optimizer、loss、梯度和显存；不追求模型效果。
- 非目标：不完成epoch、evaluation、checkpoint/resume或完整G9训练，不占用保留的GPU 6–7，不自动重跑。
- 运行：用户在fresh八卡全空报告后授权物理GPU 0–5。配置为30帧、512×512、BF16、24-block activation checkpointing、每卡detection batch 1和Caption batch 2；固定`MAX_OPTIMIZER_STEPS=3`。tmux为`g9_run5_six_gpu_observation:train_3steps`。

已确认：六个rank的checkpointing断言全部通过，3/3个optimizer steps完成，训练子进程和外层观察启动器退出码均为0。三步weighted total loss为1206.7188、1132.1456、1165.8433；backbone与五头梯度范数均为有限正数。PyTorch峰值allocated约20.89 GiB/rank，最高板级观测约25.77 GiB，训练子进程墙钟384.38秒。

已确认：目标步后干净停止并跳过scheduler、evaluation和checkpoint；没有OOM、fallback、自动重跑或`epoch_*`目录。结束后tmux关闭，GPU 0–7全部回到0 MiB、0%利用率且无compute process。结果和日志位于`reports/g9/20260819_run5_six_gpu_3step_observation/`，TensorBoard位于`outputs/g9/20260819_run5_six_gpu_3step_observation/logs/`。

风险：日志存在既有球场几何invalid-value warning、`meshgrid`兼容性warning和退出时未显式destroy process group warning；本轮loss有限且退出正常，但后续长期训练前仍应分别处理。六卡全局batch不同于目标八卡基线，三步结果不能外推完整epoch稳定性或模型效果。

结论：G9由“单步可行”推进为“六卡三步可行”，完整训练仍未通过。下一步若继续G9，应只选择一个新的最小范围，例如完整短区间加checkpoint/resume；本轮授权已消耗，不自动继续。

## G9 run6六卡单epoch长跑启动

- 目的：首次验证解冻视觉主干的五头G9能否在六卡上完成完整训练epoch，并保存一个结构完整的事务式checkpoint。
- 非目标：不做evaluation、不进入第二个epoch、不宣称六卡结果等同目标八卡基线、不自动修复或重跑。

已确认：用户在最终fresh八卡全空报告后明确确认物理GPU 0–5启动，GPU 6–7保留。配置为BF16、24-block activation checkpointing、30帧、512×512、每卡detection batch 1/Caption batch 2、`MAX_EPOCHS_THIS_RUN=1`、`SKIP_EVALUATION=True`，timeout 18小时，30秒heartbeat。

已确认：epoch checkpoint已改为rank0单写、临时目录、文件大小manifest、`COMPLETE`及同文件系统原子改名；CPU假模型smoke通过成功落盘和拒绝覆盖。实际任务于2026-08-18 23:15:59 MST在tmux `g9_run6_six_gpu_one_epoch:train_epoch0`启动，六个rank均通过24/24 checkpointing断言并进入epoch 0的16,009步训练。

未知：完整epoch、scheduler step、checkpoint写入及最终退出状态尚未发生。日志为`reports/g9/20260819_run6_six_gpu_one_epoch/run.log`；失败即停且不自动重跑。

## G10 Step 3静态审查与接口smoke准备

- 目的：确认保存版TrackLab Step 3会读取和改写哪些字段，并为当前SNGS-10004 Refiner归档准备隔离的状态接口smoke。
- 非目标：不运行Step 3模块、GPU推理、评估或训练，不把接口兼容写成全链通过。

已确认：历史Step 3重新执行ReID、pitch/calibration、`apply_camera_params`、legibility、球衣号码OCR和角色识别，然后才进行场外删除、属性聚合、轨迹拼接与球队分配；原配置还启用evaluation并覆盖全部sn500。

已确认：当前Refiner checkpoint为coord-only，run2修改的业务列是`bbox_pitch`；历史Step 3的`apply_camera_params`也输出`bbox_pitch`。因此原样串接会覆盖Refiner结果，无法检验Refiner后的坐标。这是进入正式Step 3前必须解决的流程语义冲突。

已确认：固定归档仍为3,176 detections、255 images、49 tracks，并已包含Refiner保留型CPU后处理候选所需的全部声明输入列。CPU-only静态预检退出码0，结果位于`reports/g10/20260819_step3_static_preflight/`。空pipeline的TrackLab load/save接口smoke配置和manifest已经准备，但尚未运行。

下一步：单独运行一次空pipeline接口smoke并停止；随后由用户在“历史Step 3精确语义但重算坐标”和“保留Refiner坐标的CPU后处理变体”之间作出选择。

### Step 3空pipeline接口smoke run1失败

已确认：用户明确继续后启动一次CPU-only接口smoke。worker在Hydra compose阶段退出码1，错误为找不到`state/save`。原因是新launcher使用`--config-path`替换了TrackLab package配置根；实际搜索路径不含保存`state/save.yaml`的`pkg://tracklab.configs`。既有成功G10 launcher使用`--config-dir`附加本地配置目录。

已确认：失败发生在dataset、TrackerState和输入ZIP加载之前；没有Step-3模块、GPU、评估、训练、输出状态、fallback或自动重跑。日志与结构化结果位于`reports/g10/20260819_step3_interface_smoke_run1/`。

下一步：新建隔离run2并只修正Hydra目录参数，使用全新运行路径完成CPU静态预检；不复用或覆盖run1现场，不自动执行run2。

### Step 3空pipeline接口smoke run2静态准备

已确认：run2使用全新的config、manifest、launcher、worker、output、cache和report路径；run1失败现场保持不变。相对run1原因的唯一运行语义修正为使用Hydra `--config-dir`附加本地配置目录。pipeline仍为空，固定输入仍为3,176 detections、255 images、49 tracks的Refiner run2归档。

已确认：CPU-only预检在120秒timeout内退出码0，结果为`reports/g10/20260819_step3_interface_smoke_run2_preflight/result.json`。run2尚未授权或执行，没有Hydra compose、dataset、TrackerState load/save、GPU、Step-3模块、评估或训练。

下一步：用户确认后一次性授权并运行run2；5400秒timeout、30秒heartbeat、失败不重跑，完成后停止报告。

### Step 3空pipeline接口smoke run2通过

已确认：用户明确授权后，CPU-only run2通过`--config-dir`完成Hydra组合、固定SNGS-10004 dataset实例化、空Pipeline验证、TrackerState load/save。worker和launcher退出码0，总墙钟30.54秒；没有GPU、Step-3模块、评估、训练、fallback或重跑。

已确认：输出`.runtime/g10/sngs10004_step3_interface/run2/states/sn-gamestate.pklz`为40,023,528 bytes，包含`summary.json`和两份10004 pickle。输出仍为3,176 detections、255 images、49 tracks；输入输出DataFrame逐列、dtype、索引和值精确一致。结果与日志位于`reports/g10/20260819_step3_interface_smoke_run2/`。

结论：当前Refiner归档能够无损通过TrackLab Step-3状态接口；这不代表Step-3后处理已运行，也不证明坐标或角色/球队字段正确。

下一步：静态准备保留Refiner `bbox_pitch`的CPU后处理变体，排除所有会重算坐标或调用模型的模块；不在同一阶段直接运行。

## Refiner保留型Step 3 CPU后处理首轮

已确认：CPU-only脚本逐个执行7个后处理模块，排除重复标定和模型推理。首次导入因OpenMIM写只读缓存失败；精确抑制该未使用目录创建后一次重跑退出码0。结果和两张图位于`reports/g10/20260819_step3_refiner_preserving_cpu/`。

已确认：3,176条检测和全部`bbox_pitch`原值保留，场外删除0行，无同帧合并冲突。号码合并没有合并原始轨迹；ReID合并单独把49 tracks降至32，形成9个多ID组，最大组包含6个原始ID。角色仍全部为player。球队KMeans输出left/right 1,707/1,469行、20/12 tracks，但坐标仍全部位于x=-50.4911至-16.6466米，不能视为球队正确。

结论：该版本只保留为诊断，不进入生产伪标签。下一步生成9个ReID合并组的代表人物框接触图，决定禁用还是收紧ReID合并。

### Step 3 ReID合并组复核

已确认：CPU-only生成9组总览、九张逐组接触图和两两embedding距离。9组中4组含距离超过0.1的成员；最大6轨迹组最大距离0.15495、6对超过阈值，证明迭代实现存在链式合并。

已确认：目视至少发现一组明确错误，new ID 16把疑似39号与明确14号合并。new ID 3对应的原始track 3/24内部又出现酒红与蓝色球衣切换，表明上游轨迹本身已有身份污染。

结论：不保留`concat_by_reid(threshold=0.1)`。结果与图位于`reports/g10/20260819_step3_refiner_preserving_cpu/`。下一步直接运行禁用该模块的CPU消融，其他模块不变。

### Step 3禁用ReID合并消融

已确认：仅移除`concat_by_reid`后CPU运行退出码0；首次同目录脚本导入方式错误未产生输出，修正后一次重跑通过。3,176条检测、49 tracks和全部坐标原值保留，无删除、跨轨合并或同帧冲突。

已确认：球队输出变为right 37/left 12 tracks、2,598/578行；角色仍全部player，坐标仍只在左半场。带ReID版本看似更均衡的20/12建立在错误合并上，不能作为质量收益。结果与图位于`reports/g10/20260819_step3_no_reid_ablation/`。

结论：保留禁用ReID这一结构决定，但当前球队模块不保留为可信标签。下一步在相同49 tracks上替换为已经验证的跨帧球衣颜色分组。

### Step 3球队颜色替换回放

已确认：CPU-only回放在禁用ReID合并后的同一49条轨迹上完成。因命令会话编排重复，实际有两次同配置运行完成：退出码均为0，墙钟363.40/366.53秒，峰值RSS 801,064/800,740 KiB，核心指标完全相同。号码拼接虽重编号604行，但原始49条轨迹与新ID保持严格一一对应；本轮没有修改track_id、role、`bbox_pitch`或任何输入归档。

已确认：在39条人工确认的外场轨迹上，Step 3 ReID KMeans为32/39（82.1%，balanced accuracy 73.1%），固定跨帧球衣颜色为38/39（97.4%，balanced accuracy 98.1%）。KMeans/颜色簇数量分别为37/12和34/15；语义映射后两种方法在全部49条轨迹中分歧15条。颜色法唯一外场错误为此前已标记难辨的track 20。

异常：一次超过30秒的命令返回被误判为进程已终止，但隐藏会话仍继续运行；随后启动可续接会话，造成两次等价回放并发。两次结果相同，最终JSON与图片通过针对性读取；这是执行编排重复，不是算法fallback。没有再追加验证。

风险：颜色接受层可自动接受42/49，但当前Step 3把49条轨迹全部标为player，无法可靠把门将/裁判挡在球队分配之外。因此颜色法优于KMeans的结论只针对外场球队区分，尚非生产安全的完整球队字段。

结果：`reports/g10/20260819_step3_color_team_replay/`包含机器结果、总对比图与15条冲突轨迹图。结论是保留颜色候选、停止使用ReID KMeans球队模块，但本轮不写回生产归档。

下一步：先解决角色门控，使门将/裁判能够输出unknown/abstain；随后再把颜色簇接入隔离Step-3候选归档。

### 当前Step 3到训练PKL转换

目的：优先回答“当前代码链路能否跑通”，不评价角色、球队或坐标语义质量，也不要求新预测复刻历史黄金标签。

已确认：新增`reproduction/gates/g10_current_step3_convert.py`和固定manifest，复用既有`convert_step3()`读取当前no-ReID Step 3 ZIP。CPU-only正式运行退出码0，墙钟0.81秒、峰值RSS 187,504 KiB；没有GPU、推理或训练。

已确认：输出`.runtime/g10/sngs10004_current_pipeline_conversion/run1/SNGS-10004.pkl`为339,907 bytes，包含连续1至255帧、3,176个人物实例和255帧有效相机参数。全部帧/人物字段断言通过，写出后实际pickle重载通过。

边界：历史训练PKL契约只携带人物框、角色、可读性/号码和K/R/P相机矩阵，不携带`team`、`track_id`或`bbox_pitch`。本轮证明转换链可运行，不证明标签准确，也尚未证明SoccerMaster DataLoader会消费这份新文件。

结果：`reports/g10/20260819_current_step3_conversion/`。下一步单独执行CPU DataLoader读取smoke，完成G10-C的最小消费契约检查。

### G10-C 当前产物DataLoader消费smoke

目的：验证SoccerMaster真实Detection DataLoader能否消费当前SoccerFactory输出；不运行模型forward、loss、训练或GPU，也不评价标签质量。

已确认：通过本地软链接数据视图只暴露新生成的`SNGS-10004.pkl`和既有255张sn500图片。真实`build_gsr_detection_dataloader()`以train split、30帧video模式、512×512、batch size 1、workers 0运行；随机增强关闭以固定接口检查。

已确认：正式CPU运行退出码0、墙钟61.02秒、峰值RSS 1,678,324 KiB。Dataset形成8个合法30帧片段（起点0至210，间隔30）；首个batch图像shape为`[1,30,3,512,512]`、float32、范围[-1,1]且全部有限。30帧annotation均具备检测、角色、号码、lines/keypoints目标字段，张量首维对齐；所选片段194个人物与原PKL精确对应。

异常：Matplotlib因默认home配置目录只读而自动使用`/tmp`临时缓存，未影响导入、DataLoader或结果；没有fallback或重跑。

结论：固定SNGS-10004从已准备255张图片，经Step 1、前置enrichment、Refiner、隔离Step 3、训练PKL转换，到SoccerMaster DataLoader消费端已经跑通。仍不能写成“原始比赛视频全链跑通”，因为原视频到sn500图片的切片/填充实现尚未恢复；也不证明角色、球队和坐标质量正确。

证据：`reports/g10/20260819_current_pipeline_dataloader_smoke/`。下一步若继续围绕“代码全链”，应只处理原始视频到准备图片这一剩余结构缺口。

### SNGS-10004 单场端到端可视化：CPU 阶段

目的：以同一固定片段串起原视频 lineage、SoccerFactory 各阶段机器输出和后续 SoccerMaster 五头可视化入口；本轮不训练、不评价总体准确率。

已确认：首次 CPU 运行从 Chelsea–Burnley 半场视频本地生成 255 张帧并完成全量 decoded-pixel 对应检查，随后生成 Step 1 图。首次运行在球场坐标图阶段因局部 `plt` 作用域错误退出 1；错误发生前未查询或使用 GPU。

已确认：一次局部修复后使用 continuation，只复用首次已完成的 255 张本地帧和 01–04 图，不重新提取或覆盖失败证据。continuation 退出码 0，最终 8 张 SoccerFactory/lineage 图均可打开；CPU 结果记录 mapping `clip_id=4`、255 帧对应通过以及真实 DataLoader `[1,30,3,512,512]`。

边界：首次全帧核对的精确 aggregate MAE 只存在于当时内存，没有在后续 `NameError` 前落盘；控制流和 01 图支持阈值已通过，但最终结果明确记录该限制。角色、球队、号码、Refiner 坐标仍不是独立真值。

证据：`reports/one_match/20260819_sngs10004_end_to_end/run.log`、`.runtime/one_match/20260819_sngs10004_end_to_end/cpu_result.json` 和同一报告目录下 `visuals/01...08`。SoccerMaster 五头 GPU continuation 已实现并通过语法检查，但宿主 Codex 自动审批额度在启动前耗尽，因此尚未执行，不得写成五头已跑通。


## SNGS-10004 单场五头端到端可视化

- 已确认（2026-08-20T03:35:56.390706+00:00）：新鲜 CPU 阶段和单次 GPU worker 均退出 0；使用物理 GPU 0，五个 SoccerMaster 头齐全，全部完成图片可打开。
- 已确认：CaptionClassification top-1 为 `clearance`；VideoCaption top-1 为 `ball possession`，其含义仅为固定 23 事件短语 retrieval。
- 风险：单个 30 帧 clip 不代表总体指标；未运行训练、自动重试或 holdout evaluation。
- 原始证据：`reports/one_match/20260819_sngs10004_end_to_end/result.json`、`reports/one_match/20260819_sngs10004_end_to_end/run.log`。

## SoccerFactory源码本地化接管

- 目的：为后续独立修改和复现，把固定G10链路所需源码放到工作区；不复制TB级历史outputs、权重或数据，不运行GPU/推理/训练。
- 已确认：TrackLab、sn-gamestate和Refiner核心源码已落到`vendor/soccerfactory/`。核心源目录复制前后均为963个非缓存文件、10,871,371 bytes；三个来源revision和复制边界记录在`vendor/soccerfactory/SOURCE.md`。
- 异常：首次`rsync -a`因远端owner/group映射失败退出23；只增加`--no-owner --no-group`后补全退出0。首次import被OpenMIM写只读home缓存阻塞；使用此前已验证的精确缓存抑制后退出0。
- 已确认：TrackLab main、YOLO人物检测、PRTReID、NBJW标定、legibility、Qwen号码OCR、属性聚合、球队聚类和Refiner入口均从本地vendor导入；CUDA未初始化，没有模型/checkpoint加载。证据：`reports/audits/soccerfactory_local_source_takeover_20260819.md`。
- 边界：正式G10配置仍依赖远端权重/数据，尚未以本地vendor重跑固定样本；当前只能称为源码落地和import通过，不能称为完全自包含。

### 本地资产配置预备

- 只读复核确认用户NAS已有完整解说bundle：`/mnt/nas/tianlin/SoccerMasterAssets/commentary_generation_epoch11_20260817`，58,218,439,162 bytes，既有CPU身份检查通过；运行时代码已支持`SOCCERMASTER_COMMENTARY_ASSET_ROOT`。因此不再申请把同一套Llama/Q-Former/解说checkpoint复制到本地磁盘。
- 新增`reproduction/configs/local_takeover/`，按上游原生层级规划SoccerFactory权重和数据，并提供SoccerMaster主干、Step1、enrichment及Refiner本地路径配置。4份YAML和1份JSON解析通过，配置文本无`/remote-home`引用；没有加载资产、模型或GPU。
- 批次1待复制量为26,492,433,131 bytes。新增`migrate_local_assets_batch1.py`并只读执行`--check`，退出码0：13项来源大小吻合，目标无冲突；预检时目标盘余162,991,726,592 bytes，预计复制后余136,499,293,461 bytes。精确申请现归档于`archive/docs_migration_20260819/LOCAL_ASSET_MIGRATION_REQUEST.md`；在这条记录形成时尚未获批或执行。

### 本地资产迁移批次1完成

- 用户明确批准后执行`scripts/migrate_local_assets_batch1.py`一次，批准守卫生效；13项模型权重与固定输入全部完成，主进程退出码0。远端来源只读，目标位于`.local_assets/`，没有覆盖既有正式目标、GPU或训练。
- 清单apparent size为26,492,433,131 bytes；完成后可用空间136,747,585,536 bytes。正式结果：`.runtime/local_takeover/batch1/result.json`。
- 针对性复核确认13项目标无缺失，4份YAML和1份JSON可解析且无`/remote-home`。两次只读结果汇总命令因误读JSON schema失败，修正后退出码0；不影响复制结果。
- 当前结论为“源码、固定权重和固定输入已接管，静态配置通过”；尚未用本地配置重新执行GPU forward，不能写成动态自包含复现通过。

### SoccerMaster本地资产五头动态复核

- GPU查询确认0–7均为H800、每卡空闲81,090 MiB、利用率0%，无compute进程；用户明确批准GPU7单次本地推理。
- 既有五头worker增加显式本地资产/数据视图/报告目录参数，CPU语法与本地数据视图准备退出0。正式30帧inference-only在GPU7退出0；没有训练、fallback、换卡或重跑。
- 日志明确加载`.local_assets/checkpoints/soccermaster_epoch19`中的backbone、text encoder及五个任务头。五头和5张图齐全，墙钟99.573秒，峰值allocated/reserved为6,422,995,456/8,529,117,184 bytes。
- 证据：`reports/local_takeover/20260819_soccermaster_five_heads_sngs10004/`。结论只覆盖固定片段本地推理，不覆盖完整训练或语义准确率。

### SoccerFactory本地资产Step 1动态复核

- 静态Hydra首次组合因独立config-dir缺少本地TrackLab搜索包而失败；只在本地配置增加`pkg://tracklab.configs`和`pkg://sn_gamestate.configs`后，针对性组合与预检退出0。
- 用户批准GPU7后，固定255帧Step 1 inference-only退出0，墙钟34.037秒，峰值allocated/reserved为3,689,509,888/4,756,340,736 bytes；没有训练、评估、fallback或重跑。
- 阶段事件确认TrackLab来自本地vendor，YOLO/PRTReID/HRNet来自`.local_assets`。状态归档包含255图像行、3,390检测和48轨迹。
- 历史共享环境run5为3,176检测/49轨迹；当前只确认本地接口运行，不声明数值等价或质量变化。证据：`reports/local_takeover/20260819_soccerfactory_step1_sngs10004/`。

### SoccerFactory本地enrichment动态复核

- CPU静态组合确认八阶段pipeline、刚生成的Step 1输入、标定/legibility/Qwen本地权重和全新输出目录。旧worker只接受Qwen符号链接；增加保持旧分支的本地实体目录检查后，语法和预检退出0。
- 用户批准GPU7后，255帧/3,390检测inference-only退出0，墙钟95.097秒，峰值allocated/reserved为19,076,444,672/19,639,828,480 bytes；没有训练、评估、下载、fallback或重跑。
- 输出保留3,390检测/48轨迹；`bbox_pitch`、role、team全部非空，jersey number非空1,999条，255帧相机参数齐全。证据：`reports/local_takeover/20260819_soccerfactory_enrichment_sngs10004/`。
- 结论只覆盖本地字段生成，角色、球队、号码、坐标准确率仍未知。

### 本地Refiner与SoccerFactory消费端闭环

- 用户批准GPU7后，本地Refiner coord-only进程退出0、墙钟5.006秒并写出3,390行/48轨迹结果。首次artifact检查因把六键坐标字典误当四值数组而失败；修正schema后仅CPU复用既有输出通过，未重跑GPU，误报保留在最终结果中。
- Refiner输出3,390条坐标全部有限且全部更新，track_id与行索引保留。随后CPU-only无ReID concat Step 3退出0，保留3,390行/48轨迹，无同帧冲突，并生成pipeline与球场图。
- 训练PKL转换退出0：255帧、3,390人物、255帧有效相机参数。真实SoccerMaster DataLoader消费退出0：8个合法30帧片段，首批次`[1,30,3,512,512]`、408人物。
- 统一入口：`reports/local_takeover/README.md`。结论是固定样本资产/接口接管完成；G9完整训练和字段语义质量仍未通过。
