# Backbones

New visual or temporal backbones belong here. Existing upstream implementations
remain under `soccermaster.models` during the compatibility migration. A new
backbone should document its frame-level, global, and multi-level feature
outputs before it is registered in the multi-task model.
