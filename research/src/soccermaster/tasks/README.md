# Tasks

Each new task gets one directory containing its head, loss, metrics, and brief
feature contract. Existing five-head implementations stay under
`soccermaster.models` until they are migrated one task at a time.
