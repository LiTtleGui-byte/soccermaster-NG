# Experiments

Create one directory per research question. Start from `_template/`; do not copy
the full SoccerMaster source tree into an experiment.

Each experiment owns its small config and launcher. Runtime artifacts go to
`runs/experiments/<experiment_id>/<run_id>/`.
