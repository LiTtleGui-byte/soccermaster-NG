# Archive

This directory contains the pre-restructure Gate harnesses, experiment scripts,
legacy tools, tests, configuration snapshots, and compatibility leftovers.
Nothing was deleted during the 2026-08-20 reorganization.

`docs_migration_20260819/`保存已经完成的接管方案和资产迁移申请；其中路径只代表迁移当时的状态。

`reports_legacy_text_20260819/`只保存重构前已经进入Git的小型Markdown、JSON和CSV证据；其余体积较大或可再生成的历史产物继续留在本地`runs/`。

Archived code is retained for provenance. New experiments should not copy its
per-run launchers or retry files; use `research/experiments/_template/`.
