# SoccerMaster 长期工作规则

本文件适用于 `/home/tianlin/SoccerMaster` 及其所有子目录。它规定长期安全边界和 Gate 执行纪律；当前事实与进度以 `REPRODUCTION_STATUS.md` 为准，具体判定协议以 `docs/HARNESS.md` 为准。

## 1. 固定边界

- 工作目录：`/home/tianlin/SoccerMaster`
- 原始代码、数据和权重：`/remote-home/haolinyang/sports/Soccer-Backbone`
- 原始目录永久只读：绝不能修改、删除、覆盖、编译或写入其中的任何内容。
- 所有源码、脚本、配置和文档修改只能发生在本地工作目录。
- 优先使用绝对路径；shell 命令和文件名中不得出现 Markdown 链接语法。

## 2. Python 与依赖

- 共享参考 Python：`/remote-home/haolinyang/anaconda3/envs/tracklab2/bin/python`。
- 共享环境永久只读，不得安装、升级、删除或修改包。
- 当前已验证的本地 Gate Python：`/home/tianlin/SoccerMaster/.local_envs/SoccerMaster-repro/bin/python`。
- 不依赖 `conda activate`；命令中直接使用 Python 绝对路径。
- 使用本地环境时，必须显式记录 `PYTHONPATH` 和 `LD_LIBRARY_PATH`。
- 不得通过 `PYTHONPATH` 混入其他 Conda 环境的 `site-packages`。
- 实际 Gate 环境必须与 `REPRODUCTION_STATUS.md` 的当前状态一致；不一致时停止并报告。
- `.local_envs/`、`.conda_pkgs/`、`.local_deps/` 和环境归档属于大型本地资产，不得提交。删除或清理前必须报告精确目标、大小和可恢复性并等待批准。

## 3. 安全规则

- 使用任何 `sudo` 命令之前，必须说明命令、目的、影响范围和风险，并获得用户当次明确同意；不得把已有 sudo 权限视为执行授权。
- 不读取或输出密码、SSH 私钥、Codex `auth.json`、访问令牌等秘密。
- 禁止 `git reset --hard`、`git checkout --`、`rm -rf` 及等价破坏性操作。
- 不使用 `danger-full-access`、`--yolo` 或其他绕过审批的模式。
- 保留用户已有 Git 改动；不得回滚或覆盖无关 dirty 文件。
- 大文件复制前，先报告来源、目标、大小和目标文件系统剩余空间，并等待明确批准。
- 不向原始目录、共享环境或其他用户目录安装、编译、复制或写入文件。
- 上游遗留脚本可能包含硬编码的远端读写路径；任何尚未验证过的脚本首次运行前必须静态审查输入、输出和副作用，不能仅凭文件名判断安全。审查结论未受后续修改影响时不得机械重复。

## 4. GPU、评估与训练授权

- 任何 GPU 操作前都必须重新执行 `nvidia-smi`。
- 必须报告每张相关 GPU 的显存、利用率和现有进程，然后等待用户明确批准。
- 一次批准只覆盖当次说明的设备、Gate、命令范围和重跑策略；不自动授权后续 Gate。
- 与其他用户 GPU 进程共存时，必须说明额外显存预算和 OOM/性能风险。
- 未经明确批准，不运行 GPU 推理、评估或训练。
- 未经明确批准，不启动任何训练、backward、optimizer 或 scheduler。
- Gate 失败后不得盲目自动重跑；只有事先约定的安全 fallback 才能使用。

## 5. Gate 执行纪律

- 执行、修改或判定 Gate 前，必须完整阅读 `docs/HARNESS.md`。
- 每次只推进一个 Gate；完成后报告并停止，不自动进入下一 Gate。
- 只读分析和文档维护不算推进 Gate；运行验证、改变状态或形成通过/失败结论才算推进。
- 不得为了成功而静默跳过资产、样本、任务头或断言，也不得擅自改变设备、精度、batch size、数据范围或 checkpoint。
- 长时间命令必须包含 `timeout`、30 秒级心跳和明确退出码。
- Gate 通过必须同时具备：可重复命令、退出码 0、机器断言、日志、输入资产记录和明确的未验证范围。
- 所有本地输出只能写入工作区内事先说明的路径；运行证据优先放在 `reports/`。

### 5.1 验证强度必须与风险相称

默认采用能回答当前问题的最小验证，不把正式 Gate 的审计强度套在普通研究迭代上。

#### A 级：普通低风险工作

适用于文档、README、HTML、可再生成的图表、标准库分析脚本、小型局部代码修改和只读总结。

- 通常只做一次与改动直接相关的检查，例如语法检查、一次实际生成或一次关键字段检查；成功后停止。
- 不默认计算 SHA256，不创建 manifest，不建立独立 audit/retry 日志，不做逐文件或逐样本哈希。
- 不叠加 AST、导入扫描、链接扫描、HTML 解析、JavaScript 检查、`git diff --check` 和输出哈希；只有某项检查对应具体已知风险时才执行。
- 可再生的本地 HTML、JSON、图片和日志默认不需要完整证据链。小错误直接修复并做一次针对性复核，无需保留每个失败版本。
- 普通文档和展示页面无需 smoke test；若真实目标操作本身便宜、安全，应直接执行目标操作一次。
- 普通报告默认使用简洁的 Markdown。只有内容确实需要交互筛选、复杂可视化，或用户明确要求时才生成 HTML；不得为了展示效果主动把简单报告升级成网页。

#### B 级：研究实验

适用于非 Gate 的模型推理、消融、固定子集评估和研究诊断。

- 运行前只检查可能造成错误结论或资源冲突的关键项：输入范围、模型/checkpoint、设备、输出路径和副作用。
- 一次正式运行保留一个主日志和一个结果文件通常已经足够；不得为同一运行重复制造 manifest、摘要、哈希清单和多份审计日志。
- 记录路径、配置、seed、样本范围、设备、退出码和核心指标；只有输入身份容易混淆时才使用哈希。
- 不重复验证已经由同一固定缓存、配置或 checkpoint 契约保证的逐样本身份；没有损坏迹象时，不做 200 个逐样本哈希或写后再做一套独立等价复核。
- GPU 运行前的 `nvidia-smi` 和授权仍是强制项；运行结束后只有出现 OOM、资源冲突、异常退出或需要确认清理时才再次检查，不默认反复查询。

#### C 级：严格审计

只适用于正式 Gate 通过/失败判定、checkpoint exact resume、训练、环境或大型资产迁移、不可轻易重建的数据转换、共享/原始资产变更风险，以及用户明确要求冻结证据的场景。

- 可以使用 SHA256、manifest、分阶段断言、资源峰值、完整状态恢复检查和独立复核。
- 即使属于 C 级，每项检查也必须对应明确风险；不得因为“更稳妥”无限叠加等价检查。

从 A/B 级升级到 C 级必须说明具体原因，例如疑似文件损坏、跨机器传输、正式 Gate 判定或不可逆操作。用户可以明确要求提高或降低某一轮的验证强度。

### 5.2 禁止验证表演

- 每个检查必须能回答一个具体问题；无法说明“失败将改变什么决定”的检查不执行。
- 同一事实原则上只验证一次。已有可信日志或结果时优先读取，不重新跑一遍来证明它仍然存在。
- 一处明显的小错误采用“一次修复 + 一次针对性复核”；不自动扩展成全仓库审计。
- 不为了让报告显得完整而计算哈希、生成 manifest、保存 retry 文件或重复 `git status`。
- 报告研究结果优先讲模型行为、数据含义和下一项实验，不让审计过程淹没主要结论。

## 6. 运行记录采用分级原则

A 级工作只需报告改了什么、输出在哪里以及一次必要检查的结果。B 级研究实验至少记录：

- 目的和明确非目标；
- 使用的脚本/配置、样本范围、seed 和设备；
- 退出码、核心指标和主日志/结果路径；
- 真实发生的 fallback 或异常。

只有 C 级严格审计才要求按适用范围记录以下完整信息：

- 用户授权范围和明确非目标；
- Git commit 与相关 dirty 文件；
- Python 绝对路径以及关键依赖版本；
- `PYTHONPATH`、`LD_LIBRARY_PATH`、CUDA 可见设备和 dtype；
- resolved config、checkpoint 类型和绝对资产路径；
- 固定 seed、样本 manifest、batch size 和采样策略；
- timeout、心跳、开始/结束时间和退出码；
- 峰值 CPU 内存、GPU 显存及适用的分阶段耗时；
- 断言结果、日志和机器可读产物路径；
- 是否发生 fallback，以及它对结论范围的影响。

## 7. 报告用语

报告复杂实验或 Gate 时必须区分：

- **已确认**：有命令、代码、日志或资产证据直接支持；
- **推断**：依据现有证据作出的判断，但尚未按目标协议验证；
- **未知**：证据不足；
- **风险**：可能影响正确性、资源安全或结论范围；
- **下一步**：只给出一个最小、可验证、不会影响其他用户的动作。

简单文档修改、普通问答或单一结果不需要机械列出全部五个栏目；直接给出结论即可。

不得把单样本结果写成总体指标，不得把模型权重加载写成 forward 正确，也不得把部分状态恢复描述为 exact resume。

## 8. 文档与证据职责

- `README.md`：项目入口、当前总体状态和导航。
- `AGENTS.md`：长期规则和授权边界。
- `docs/HARNESS.md`：Gate 执行模板、数据/checkpoint 契约和通过标准。
- `REPRODUCTION_STATUS.md`：唯一当前状态账本。
- `reproduction/README.md`：复现入口、Gate 脚本、manifest 和证据导航。
- `reproduction/gates/`：本地 Gate 审计入口。
- `reproduction/manifests/`：固定的小规模输入清单。
- `experiments/`：后续改进与消融实验的登记和配置；不得与已验证复现基线混用。
- `lab_notes/`：按 UTC 日期记录实际研究过程、失败、判断和写作素材；不能替代状态账本或原始证据。
- `reports/`：原始日志、JSON 和可视证据。
- `.runtime/`：可重建的临时数据视图和运行时链接，Git 永久忽略。
- `docs/future_improvements/`：尚未验证的研究假设；不得作为当前基线事实。

### 8.1 每日实验日志

- 完成重要实验、环境迁移、资产审计或形成新的研究判断后，在结束本轮前更新当天的 `lab_notes/YYYY-MM/YYYY-MM-DD.md`。
- 普通文档修改、HTML 生成、小修复和重复检查无需单独写入每日日志，除非它们改变了研究结论或暴露了会影响结果可信度的问题。
- 日志文件名使用 UTC 日期；正文中的运行时间应保留原始时区或明确标出 UTC。
- 只把实际执行过的操作写成完成；计划、建议和尚未执行的命令必须明确标为待办。
- 日志应链接 `reports/` 中的原始证据，不复制整段终端输出，不记录密码、token、私钥或其他秘密。
- 日志优先记录问题、实验设计、结果和解释；默认不记录 AST、链接扫描、`git diff --check`、普通文件哈希等过程性检查。
- 日志中的研究判断继续区分已确认、推断、未知和风险；不得用日志把未通过的 Gate 改写成通过。
- “后续计划与候选方向”允许记录多个并列想法，仅作为工作备忘，不构成 GPU、训练、复制、安装或下一 Gate 的执行授权。

## 9. Gate 顺序

1. G0 资产定位
2. G1 Python/依赖/CUDA 环境
3. G2 完整 checkpoint 加载
4. G3 随机张量 forward
5. G4 单个真实视频
6. G5 固定小规模评估
7. G6 tiny overfit
8. G7 单任务训练
9. G8 小规模多任务训练
10. G9 完整训练
11. G10 SoccerFactory 分支

前一 Gate 通过不代表后一 Gate 已授权或已通过。

### 9.1 G10 提前启动的已批准例外

- 2026-08-13，用户因 checkpointing 适配版 G9 完整 20 epochs 的预计成本达到数周，明确批准暂停扩展 G9，隔离地提前进入 G10。
- 该例外只改变当前工作顺序，不改变 Gate 结论：G9 仍为“单步可行、完整训练未通过”，不得写成通过或被 G10 替代。
- G10 的代码、配置、manifest、数据视图、输出和报告必须使用独立命名空间，不得静默修改 G8/G9 复现基线或原始伪标签。
- 在 G9 完整基线缺失期间，G10 可以验证数据质量、生成链路、训练接口和固定小规模实验；其结果不得表述为“优于完整 G9”或论文级总体提升。
- G10 内仍按最小阶段逐步推进，每个阶段完成后报告并停止；任何 GPU、推理、评估或训练仍需新的当次授权。

## 10. 新会话接管

新的 Codex 账号或会话接管本项目时，必须按以下顺序开始：

1. 确认当前工作目录是 `/home/tianlin/SoccerMaster`。
2. 完整读取本文件。
3. 完整读取 `REPRODUCTION_STATUS.md`；它是唯一当前状态账本。
4. 只读执行一次 `git status --short --branch`，保留已有 dirty 文件。
5. 只有当前任务涉及执行、修改或判定 Gate 时才完整读取 `docs/HARNESS.md`；普通问答、文档和独立研究分析不重复读取。
6. 执行状态账本中的下一步或用户新明确指定的任务，不从头规划，不自动扩大范围。

如果账本中的下一步与实际文件或 Git 状态冲突，应停止并报告，不得自行猜测。
