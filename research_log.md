# Research Log

这里记录正在推进的研究闭环。每项只保留：假设、失败、原因、修改、结果和去留；正式 Gate 状态仍以 `REPRODUCTION_STATUS.md` 为准，详细过程按需写入 `lab_notes/`。

## 2026-08-19｜SoccerFactory本地源码接管

- 假设：只复制固定G10链路实际import的package、plugins和Refiner推理闭包，可以摆脱TB级历史outputs而保留可修改源码。
- 失败现象：首次`rsync -a`因远端`nobody:nogroup`无法映射到本地group而退出23；首次本地import又被OpenMIM写只读home缓存阻塞。
- 原因判断：前者是元数据继承问题，后者是已知的未使用缓存副作用，都不是文件内容或依赖缺失。
- 修改内容：复制时不继承owner/group；本地import审计只抑制精确的`/home/tianlin/.cache/mim`创建，未加载模型或GPU。
- 结果变化：`vendor/soccerfactory/`落地963个核心文件、10,871,371 bytes；TrackLab、8个sn-gamestate关键模块和Refiner入口均确认从本地路径导入。
- 是否保留：保留本地vendor和来源说明；下一步迁移获批的权重/固定数据并把正式配置改成本地路径，未完成前不称为自包含运行。

## 2026-08-19｜本地资产目录与配置预备

- 假设：按上游原生目录层级放置权重和数据，可以只改根路径而不改各模块加载语义；现有用户NAS解说bundle可避免重复复制约58GB。
- 失败现象：原迁移草案把SoccerFactory权重摊平成单层文件，不符合`${model_dir}/reid|calibration|jn`等上游路径约定；同时遗漏了已存在的用户NAS完整解说bundle。
- 原因判断：最初按资产类型盘点，没有同时按运行时目录契约核对；解说资产状态散落在独立NAS预检报告中。
- 修改内容：把目标调整为原生`pretrained_models`层级；新增`reproduction/configs/local_takeover/`路径注册和主干/Step1/enrichment/Refiner配置，复用用户NAS SigLIP2及解说bundle。
- 结果变化：4份YAML和1份JSON解析通过且无`/remote-home`引用；批次1新复制量由30,060,458,305降为26,492,433,131 bytes。
- 是否保留：保留目录和配置设计；大资产仍等待批准，未运行模型。

## 2026-08-19｜保留Refiner坐标的Step 3后处理

- 假设：跳过重复标定和模型推理，只运行CPU后处理，可以保留Refiner坐标并完成轨迹合并与球队分配。
- 失败现象：坐标虽完整保留，但ReID合并把49条轨迹压到32条，9个合并组中最大一组包含6个原始ID；角色仍为3176/3176个player。最终球队看似变为left 1707/right 1469，但全部坐标仍落在球场左半区，不能把分布变均衡当成正确。
- 原因判断：`concat_by_reid`的0.1阈值是轨迹数骤降的唯一来源；上游角色证据本身已塌缩，后处理投票无法恢复门将/裁判。球队KMeans只会强制分成两簇，不提供语义正确性保证。
- 修改内容：排除ReID、标定、OCR和角色模型重跑，逐模块执行7个CPU后处理并记录删除/合并/字段变化；生成模块变化图和最终球场轨迹图。首次导入被OpenMIM只读缓存写入阻塞，精确抑制该未使用缓存目录后一次重跑通过。
- 结果变化：0条检测被删除；号码合并没有真正合并原始轨迹；ReID合并产生9组、49→32 tracks；号码非空由1801增至1948；最终左右队为20/12 tracks。`bbox_pitch`逐值不变，无同帧轨迹冲突。
- 是否保留：仅保留为诊断版本，不进入生产伪标签。下一步优先可视检查9个ReID合并组，决定禁用该模块还是收紧阈值；角色和坐标半场塌缩需独立解决。

## 2026-08-19｜Step 3 ReID轨迹合并复核

- 假设：`concat_by_reid`在0.1阈值下合并的是同一球员被切断的轨迹。
- 失败现象：接触图出现明确错误合并；new ID 16把疑似39号与明确14号合并。new ID 3的原始track 3/24内部已在酒红与蓝色球衣之间切换，说明输入轨迹本身含身份污染。9个最终组中4组包含原始两两距离大于0.1的成员。
- 原因判断：模块采用迭代局部最近合并，允许链式传播，不能保证最终组内任意两条轨迹都低于阈值；同时输入track已可能发生ID switch，均值ReID特征会进一步混合身份。
- 修改内容：为9组合并结果生成总览、每组最多3个时间点的人物框接触图，并计算所有原始轨迹均值特征的两两余弦距离；未重新运行模型。
- 结果变化：确认4/9组存在阈值外成员；最大组6条原始轨迹的最大距离0.155，且有6对超过0.1。至少new ID 16可目视确认不应合并。
- 是否保留：不保留当前`concat_by_reid(threshold=0.1)`；下一版Step 3先完全禁用该模块，保留49条原始轨迹。收紧阈值只有在身份真值或更可靠tracklet输入上再研究。

## 2026-08-19｜Step 3禁用ReID合并消融

- 假设：删除不可靠的ReID合并可以保留身份结构，同时不影响其余CPU后处理运行。
- 失败现象：49条轨迹和坐标均被安全保留，但最终球队分配变为right 37/left 12 tracks（检测行2598/578）；角色仍全部为player，坐标仍只覆盖球场左半区。
- 原因判断：删除ReID避免了错误身份合并；但现有球队KMeans本身仍不可靠。带ReID版本看似更均衡的20/12只是合并改变了聚类样本，并非球队识别改善。
- 修改内容：保持场外过滤、两次属性聚合、号码合并和球队模块不变，只移除`concat_by_reid`，并生成有/无ReID的轨迹数与球队分布对比图。
- 结果变化：轨迹32→49；无删除、无跨轨合并、无同帧冲突，`bbox_pitch`逐值不变。号码非空仍为1801，没有因错误合并扩散到1948。
- 是否保留：保留“禁用ReID”的结构性决定；当前球队与角色输出仍不进入生产。下一步把现有KMeans球队模块替换为已经验证过的跨帧球衣颜色分组，在相同49条轨迹上回放。

## 2026-08-19｜Step 3球队颜色替换回放

- 假设：在禁用ReID合并后的同一49条轨迹上，跨帧上半身球衣颜色比Step 3的ReID KMeans更可靠。
- 失败现象：现有KMeans在39条人工确认的外场轨迹上仅32/39正确；两种方法在全部49条轨迹中有15条分歧。执行编排误判30秒返回为进程终止，意外产生两次同配置CPU回放；两次退出码均为0且核心指标完全相同。
- 原因判断：ReID特征主要服务身份，不稳定地编码球队颜色；固定颜色特征直接使用球衣区域。颜色法唯一错误是难辨的track 20。
- 修改内容：新增CPU-only逐轨回放和两张比较图；通过一一轨迹映射处理号码拼接造成的ID重编号。人工标签只用于两种无监督结果生成后的评价，没有写回归档。重复执行只写同一可再生报告目录，未触碰源状态。
- 结果变化：KMeans 32/39（82.1%，balanced 73.1%）→颜色38/39（97.4%，balanced 98.1%）；颜色簇34/15，安全接受层42/49。
- 是否保留：保留颜色法作为球队候选，放弃ReID KMeans球队模块；暂不写回生产字段，因为当前角色全为player，仍无法可靠排除门将/裁判。

## 2026-08-19｜当前Step 3到训练PKL转换

- 假设：当前no-ReID Step 3输出能够沿用已恢复的历史转换契约，生成SoccerMaster侧的逐序列训练PKL。
- 失败现象：无；一次CPU正式运行退出码0。
- 原因判断：当前ZIP仍满足转换器所需的检测、图像和相机字段，预测数量变化不影响结构转换。
- 修改内容：新增薄转换入口和固定manifest，复用既有`convert_step3()`；不与4,260人物的历史黄金标签强行比较。
- 结果变化：成功生成255帧、3,176个人物、255帧有效相机参数的339,907-byte PKL，写后重新读取通过。
- 是否保留：保留，固定样本链路已到最终训练PKL；下一步只验证SoccerMaster DataLoader能否消费该文件。

## 2026-08-19｜当前SoccerFactory产物的DataLoader消费

- 假设：SoccerMaster真实Detection DataLoader能够直接消费当前链路生成的逐序列PKL和对应图片。
- 失败现象：无；仅有Matplotlib因home只读自动使用`/tmp`缓存的非致命提示。
- 原因判断：新PKL满足现有lazy extra-data读取契约，人物字段和相机矩阵能被转换为训练张量。
- 修改内容：建立只含一个序列的本地软链接数据视图，使用真实30帧video模式、512×512预处理、batch size 1、workers 0并关闭随机增强。
- 结果变化：真实DataLoader产出`[1,30,3,512,512]` float32有限张量；30帧194个人物与PKL逐帧对应，全部字段/shape/dtype/CPU断言通过。
- 是否保留：保留；从已准备255张图片开始到SoccerMaster训练消费端的固定样本链路已跑通。原始比赛视频到255张准备图片的缺失边界仍未恢复。

## 2026-08-19｜外场球队颜色分组

- 假设：跨帧上半身颜色比现有 ReID 球队簇更适合区分两队外场球员。
- 失败现象：现有 ReID 在第一场聚类塌缩，第二场又因角色硬门产生覆盖缺口。
- 原因判断：球队颜色本身可分；ReID 聚类和上游角色过滤不稳定。
- 修改内容：实现只读颜色特征回放，并把原始颜色簇与安全接受层分开。
- 结果变化：两场73条外场轨迹中颜色结果71/73正确（97.3%）；安全接受层60/73且60/60正确。
- 是否保留：保留颜色分组；暂不接生产链，先解决角色门控。

## 2026-08-19｜门将/裁判启发式角色门控

- 假设：角色时序、颜色离群和球场位置可以在不训练模型时拦截门将/裁判。
- 失败现象：严格判断只命中2/12；扩大复核命中11/12，但误拒16/73条外场球员。
- 原因判断：召回主要来自大范围拒绝，不是角色识别变准；短轨迹和颜色离群缺乏角色特异性。
- 修改内容：固定规则双场离线回放，不根据人工结果调阈值。
- 结果变化：严格recall 16.7%；扩大拦截recall 91.7%、外场误拒率21.9%。
- 是否保留：放弃作为生产角色门；停止在相同两场继续调规则。

## 2026-08-19｜`role_confidence`加权

- 假设：逐帧角色置信度加权可能比普通多数投票更可靠。
- 失败现象：两场89条轨迹中，加权与不加权投票0条不同；12条非外场只识别2条。
- 原因判断：PRTReID保存原始最大logit，Qwen保存恒为1的标签有效性指示；同名字段语义不一致。部分PRTReID轨迹完全没有非player top-1证据。
- 修改内容：复现轨迹聚合并审计两种角色模块的源码与归档分布。
- 结果变化：确认PRTReID场次8条非外场全部聚合为player；现有归档又缺少完整类别分数，无法事后校准。
- 是否保留：不保留统一置信度加权方案；后续使用生产者专属分数字段。

## 2026-08-19｜同场只替换Qwen角色模块

- 假设：在固定SNGS-10004人物框和轨迹后，仅把PRTReID角色输出替换为Qwen2.5-VL，可以显著改善门将/裁判识别。
- 失败现象：Qwen明确识别出3/8条门将/裁判；其余5条门将多被输出为`other`。若把`other`也当非外场，会误拒25/39条外场球员。
- 原因判断：现有跨场差异确实有角色模型因素；Qwen能识别清楚的裁判和门将，但单个小框/模糊帧常触发`other`，该标签同时混合低画质外场球员和真实非外场。
- 修改内容：固定49条轨迹和137个跨时间视图，用相同全帧+人物框提示运行Qwen2.5-VL，并在写出候选后读取人工标签评价。
- 结果变化：明确`goalkeeper/referee`由PRT的0/8提升至Qwen的3/8，外场误拒保持0/39；宽松包含`other`虽为8/8，但外场误拒率64.1%。
- 是否保留：保留Qwen明确角色作为高精度证据，不保留`other=非外场`规则；下一候选是一次输入整条轨迹的多视图提示。

## 2026-08-19｜Qwen轨迹级多视图提示

- 假设：一次性查看同一轨迹最多3个时间点，可以用清楚视图覆盖模糊视图，提高门将/裁判明确召回。
- 失败现象：多视图只命中2/8，低于单帧投票的3/8；虽新识别门将42，却丢失裁判4、17。
- 原因判断：当前通用提示把大多数困难轨迹统一推向`player`，多张静态图没有形成稳定的角色时序推理；增加图片本身不能补足小目标角色证据。
- 修改内容：固定原137个视图，每条轨迹把最多3组整帧+人物框放入一次Qwen提示，不调阈值。
- 结果变化：明确角色precision 100%、recall 25%、外场误拒0/39；单帧对应recall为37.5%。
- 是否保留：不保留当前多视图提示；继续保留单帧Qwen明确角色作为高精度补充证据。

## 2026-08-19｜Refiner坐标时间连续性

- 假设：coord-only Refiner在保持合法球场坐标的同时，会降低同一轨迹的逐帧抖动。
- 失败现象：二阶差分中位数由0.213米升至0.401米，38条可比较轨迹中35条变差。
- 原因判断：100/200帧的独立clip边界会放大跳变，但只覆盖1.84%的抖动样本；去掉边界后，中位数仍由0.212米升至0.397米，说明主要是段内residual逐帧变化，不是单纯拼接问题。
- 修改内容：增加Refiner前后坐标、轨迹连续性与clip边界分层诊断，并生成逐帧抖动图；未运行GPU或Step 3。
- 结果变化：结构契约安全，3,176个坐标均有限且无新增越界；但没有观察到时间平滑收益。跨边界步长中位增量+0.354米，段内仍为+0.075米。
- 是否保留：保留Refiner输出用于后续接口链路smoke；不把它表述为坐标质量改进，也不继续把重叠clip当作首要修复方向。绝对精度需带二维真值的新样本评价。

## 2026-08-19｜Q-Former两层信息筛查

- 假设：如果两层Q-Former不足以保留多事件事实，Q-Former输入到输出的可读信息下降应集中出现在事件、动作或结果任务。
- 失败现象：17个已有任务中，事件平均下降0.055、结果平均下降0.073，动作平均下降0.020；但phase两项反而上升0.021，不能据此证明“层数太少”。
- 原因判断：两层压缩可能损失部分事件/结果信息，但任务差异和临时视频标签噪声也能产生同样现象；当前结果不是深度的因果实验。
- 修改内容：新增CPU-only汇总脚本和一张逐任务输入/输出差值图，不新增probe、不运行GPU、不修改模型。
- 结果变化：`result:saved`、`event:possession_sequence`和`event:shot_attempt`下降最大；`action:cross`、`phase:replay`上升。结果只保留为深度候选筛查。
- 是否保留：保留图和筛查；下一步若取得GPU授权，优先做固定下游的2层/更深Q-Former小规模对比，而不是继续扩展hidden-state解释。

## 2026-08-18｜ID Switch候选回放

- 假设：保存的ReID特征、相邻帧位置变化和字段冲突可以先筛出需要人工检查的ID Switch时刻。
- 失败现象：连续帧的最高异常分数很低，不能直接证明发生了切换；但49条轨迹中41条出现过多帧间断，最长间断216帧。
- 原因判断：当前归档没有人物身份真值，且短轨迹/长间断会把“重新出现”和“换人”混在一起。
- 修改内容：新增CPU-only候选筛选和时间线图，分别分析连续帧过渡与间断后重现，不改写原始track_id。
- 结果变化：得到20个间断后重现候选；最高候选为track 5在0→43帧、track 4在23→239帧，均只能作为人工复核入口。
- 是否保留：保留筛选器和图；下一步只人工查看前5个间断候选，确认后再决定是否切轨迹。

## 2026-08-19｜Q-Former层1早退对照

- 假设：如果当前两层Q-Former的第二层是主要信息瓶颈，直接让下游使用第一层输出会明显改变或破坏解说；这可作为是否值得做更深Q-Former的快速筛查。
- 失败现象：第一层早退和第二层输出在12/12条固定视频上都改变了文本；第一层有多条乱码、重复数字或明显错误结果，第二层文本整体更完整，但仍有少量事实错误。
- 原因判断：第一层表示尚未适配现有projector，早退退化不能等价于“训练一个一层Q-Former”；因此只能说明第二层变换对当前下游接口很重要，不能单独证明层数太少。
- 修改内容：固定12条高置信、可观察开发视频；注册Q-Former第1层和最终层hook；保持同一视觉backbone、projector、Llama和确定性beam decoder，分别生成两份文本，并保存中间张量。
- 结果变化：12/12文本不同；平均层1/层2范数为156.81/156.87，平均层间差范数98.52；GPU峰值分配约18.2GiB，运行154秒。输出见`reports/commentary_qformer_layer_exit_12_20260819/`。
- 是否保留：保留为“第二层对现有下游接口不可省略”的证据；不把它写成“Q-Former层数不足”的结论。下一步只做12条输出的盲事实复核，再决定是否值得申请一次interface-matched的一层/三层对照。

## 2026-08-19｜原视频到准备图片的来源核对

- 假设：固定 `SNGS-10004` 的 255 张图片来自仓库中的 `segment_soccernet.py`，可以恢复原视频入口。
- 失败现象：旧 G10 manifest 把 `SNGS-10004` 关联到 mapping `clip_id=10004`（Southampton–Liverpool，`14439–14645`，207 个索引），但候选脚本的命名规则是 `SNGS-{clip_id+10000:05d}`。
- 原因判断：当前 `sn_2_clip.json` 的 `clip_id=4`（Chelsea–Burnley，`2278–2532`）才会生成 `SNGS-10004`，而准备图片首帧与该原视频候选的平均绝对像素差约 `0.85`，与 manifest 候选约 `37.84`；旧清单的编号语义不可靠，可能是把序列后缀误当成 mapping id。
- 修改内容：静态审查本地/远端切片脚本、映射文件和源视频；做一次 CPU 只读首帧比较；新增候选清单 `reproduction/manifests/g10_raw_video_lineage_candidate_sngs10004.json` 和 `reports/g10/20260819_raw_frame_source_check/result.json`，更新资产表与 SoccerFactory pipeline map。没有运行会写远端 `sn500`/`fail.txt` 的历史脚本。
- 结果变化：原视频切片实现候选已定位，源视频和相机标注资产存在；但固定样本的 manifest/report 与实际图片来源出现矛盾，原视频端到端仍未验证。
- 是否保留：保留候选脚本和像素证据；先修订固定 lineage 清单并写一个隔离本地单片段复现入口，再决定是否恢复完整切片。

## TODO｜全景视角检测与跟踪对照（暂不执行）

- 问题：固定全场俯拍可能减少镜头切换、出画和轨迹中断，但球员变小也可能降低人物检测、ReID 和号码识别效果。
- 候选数据：SoccerTrack v2 的 4K 全场全景完整比赛及逐帧人物框、ID、球队、角色和球场坐标真值。
- 最小实验：固定一分钟视频，对比“整图缩放到 512×512”和“保持高分辨率分块检测后合并”，分别报告人物召回、ID Switch 和轨迹连续性。
- 当前决定：只登记待办，不下载数据、不适配代码、不运行实验；先逐张理解现有可视化。

## TODO｜明确复现 F04→F05 的球场标定与反投影（计划下周）

- 问题：目前还没有非常明确地掌握球场线/关键点如何解码、相机参数如何求得，以及人物框底边中点如何得到二维球场坐标。
- 最小目标：沿真实代码和一个固定帧，逐步保存 `SV_lines/SV_kp` 输出、解码后的线与点、相机 `K/R/T`、人物像素脚点、射线与 `z=0` 平面的交点，并手算复核一个人物坐标。
- 判定：让 F04 中的一个标定点和 F05 中的一个人物点都能从输入到输出逐项解释；同时记录误差会从哪一步传递和放大。
- 当前决定：只登记为下周待办，本轮不执行；继续逐张理解现有可视化。

## 2026-08-19｜SNGS-10004 单场自动可视化（CPU 阶段）

- 假设：`clip_id=4` 的原视频帧可在本地复现既有 255 张准备图片，并可把已有 SoccerFactory 各阶段结果统一渲染为可审阅图片。
- 失败现象：首次运行完成 255 帧全量对应检查和 Step 1 图后，`render_geometry()` 因 `plt` 只在前一函数局部导入而触发 `NameError`；GPU 尚未查询或使用。
- 原因判断：单一可视化脚本作用域错误，与数据、模型或 lineage 无关；首次运行已进入后续渲染阶段，说明全帧对应阈值已通过。
- 修改内容：把 Matplotlib 导入移到模块作用域，增加只复用首次已完成帧和 01–04 图的 CPU continuation；没有重提取、重跑全帧核对或覆盖失败证据。
- 结果变化：continuation 退出码 0；8 张图全部可打开，mapping `clip_id=4`，255 帧对应通过，最终 PKL/DataLoader shape 为 `[1,30,3,512,512]`。结果在 `.runtime/one_match/20260819_sngs10004_end_to_end/cpu_result.json`，主日志在 `reports/one_match/20260819_sngs10004_end_to_end/run.log`。
- 是否保留：保留 CPU 结果和一次失败日志；当前只确认 lineage、接口与可视化，不把角色、球队、号码或坐标字段解释为真值。SoccerMaster 五头 GPU inference 因宿主 Codex 审批额度阻塞，尚未运行。


## SNGS-10004 单场五头可视化

- 实际执行：SNGS-10004 CPU 准备与一次五头 inference-only GPU worker 均成功，物理 GPU 0。
- 结果变化：五个头齐全；分类 top-1 为 `clearance`，VideoCaption 固定短语 retrieval top-1 为 `ball possession`。
- 去留决定：保留结果；VideoCaption 仅解释为 23 类短语检索，不解释为生成解说。证据：`reports/one_match/20260819_sngs10004_end_to_end/result.json`。

## 2026-08-19｜本地资产迁移批次1只读预检

- 假设：按精确清单迁移主干checkpoint、SoccerFactory权重和固定样本后，可以摆脱对他人远端资产路径的运行依赖。
- 失败现象：尚未获得约24.7 GiB正式复制授权，因此当前仍不能称为本地自包含复现。
- 原因判断：代码与配置已经就绪，剩余阻碍是大型资产复制需要用户明确确认，不是代码或磁盘空间问题。
- 修改内容：新增带批准开关、源大小核对、空间下限、`.partial`临时目标和原子改名的批次1迁移脚本；只运行`--check`，未复制任何资产。
- 结果变化：13项来源与预期大小全部吻合，目标无冲突；总量26,492,433,131 bytes，预检时可用162,991,726,592 bytes，预计完成后剩余136,499,293,461 bytes。
- 是否保留：保留迁移清单与脚本；等待明确批准后执行一次正式复制，失败不自动换来源或重跑。

## 2026-08-19｜本地资产迁移批次1正式执行

- 假设：批准批次中的13项资产可以按原生目录结构落地，并让接管配置摆脱师兄的运行路径。
- 失败现象：迁移本身无失败；完成后的首次结果读取把正式JSON误当成预检结构，随后又误用了不存在的`expected_bytes`字段，两次均为只读报告命令错误。
- 原因判断：正式结果用`copies/content_identity`，而预检输出用`assets/bytes`；资产复制进程已先于这两次读取正常退出0。
- 修改内容：经用户明确批准，使用批准守卫执行一次批次1迁移；13项逐一写入`.partial`、核对内容身份并原子改名。随后按真实schema检查全部目标和5份本地配置。
- 结果变化：清单apparent size 26,492,433,131 bytes；13项目标无缺失，5份配置可解析且无`/remote-home`，完成后可用空间136,747,585,536 bytes。没有GPU、训练或远端写入。
- 是否保留：保留全部13项本地资产和迁移结果；下一步仅在新GPU授权下做固定样本的本地配置inference-only动态复核。

## 2026-08-19｜SoccerMaster本地资产五头动态复核

- 假设：迁移后的`epoch_19`、用户NAS SigLIP2和本地255帧数据视图足以脱离师兄运行路径完成固定片段五头forward。
- 失败现象：无模型失败；仅有Albumentations版本提醒和Torch未来兼容提醒。
- 原因判断：backbone、text encoder和五个头均从本地checkpoint成功加载，迁移后的主干推理闭包完整。
- 修改内容：把既有五头worker的模型、checkpoint、数据视图和报告目录改为显式环境参数；准备只链接本地图片和本地PKL的新数据视图。经授权在物理GPU7运行一次30帧inference-only。
- 结果变化：退出码0、五头齐全、5张图落盘；墙钟99.573秒，峰值allocated/reserved为6.42/8.53GB，`training=false`。
- 是否保留：保留参数化入口和结果；SoccerMaster固定推理资产接管成立，但不外推到完整训练或总体准确率。下一步转向SoccerFactory本地Step 1动态复核。

## 2026-08-19｜SoccerFactory本地资产Step 1动态复核

- 假设：vendored TrackLab/sn-gamestate、本地YOLO/PRTReID/HRNet和本地255帧足以脱离师兄路径完成Step 1。
- 失败现象：首次静态Hydra组合找不到`bpbreid_strong_sort`；动态运行无失败，但输出3,390条检测/48轨迹，与历史共享环境run5的3,176/49不完全一致。
- 原因判断：独立config-dir缺少vendored TrackLab配置搜索路径；补齐后组合成功。数值差异可能来自Python/依赖运行时差异，当前没有证据定位到单一原因。
- 修改内容：在本地配置显式加入TrackLab/sn-gamestate Hydra搜索包；新增带GPU批准守卫、30秒心跳、失败即停和单一结果的本地Step 1入口。经授权在GPU7运行一次255帧inference-only。
- 结果变化：退出码0、墙钟34.037秒、峰值allocated/reserved 3.69/4.76GB；本地vendor与3个本地权重读取均由阶段事件确认。
- 是否保留：保留本地Step 1入口和结果，结论为接口可运行；不保留“与历史数值等价”结论。下一步静态准备本地enrichment/Refiner后半链。

## 2026-08-19｜SoccerFactory本地enrichment动态复核

- 假设：迁移后的标定、legibility和Qwen权重可以直接消费新本地Step 1归档并生成Refiner所需字段。
- 失败现象：无动态失败；本地Qwen是实体目录，而旧worker只接受历史符号链接契约。
- 原因判断：这是资产布局差异，不是模型差异；对旧符号链接分支保持原逻辑，同时为本地实体目录增加严格目录检查即可。
- 修改内容：本地enrichment配置加入vendor Hydra搜索路径；新增本地资产guarded launcher，并让既有worker显式支持非符号链接Qwen目录。经授权在GPU7运行一次全255帧/3,390检测inference-only。
- 结果变化：退出码0、墙钟95.097秒、峰值allocated/reserved 19.08/19.64GB；3,390条均生成坐标/角色/球队，1,999条号码非空，255帧均有相机参数。
- 是否保留：保留本地入口和结果；只证明字段生成，不声明标签准确。下一步是本地Refiner coord-only动态复核。

## 2026-08-19｜SoccerFactory本地Refiner与消费端闭环

- 假设：迁移后的Refiner源码/checkpoint可以消费本地enrichment归档，并继续进入保留版Step 3、训练PKL和SoccerMaster DataLoader。
- 失败现象：GPU模型进程退出0后，首次检查器把六字段`bbox_pitch`字典当成四值数组，误报全部坐标非有限。
- 原因判断：实际坐标字典六个值均有限，输入/输出行与轨迹ID一致；失败完全来自检查器schema假设错误，不是模型输出错误。
- 修改内容：检查器改为验证六个明确坐标键；只复用既有输出做一次CPU复核，没有重跑GPU。随后运行无ReID concat的CPU Step 3、PKL转换和真实DataLoader消费。
- 结果变化：Refiner GPU进程墙钟5.006秒，3,390条坐标全部更新且48轨迹保留；Step 3保留3,390行/48轨迹且无同帧冲突；PKL为255帧/3,390人物；DataLoader首批次`[1,30,3,512,512]`。
- 是否保留：保留本地全链入口和结果；资产接管目标完成。仍不保留“Refiner改善坐标质量”或“字段语义准确”的结论。
## 2026-08-19｜本地代码与 checkpoint 分层

- 假设：保留已验证物理路径，同时增加按所有权和用途分类的规范入口，可以降低后续实验混用代码、模型和结果的风险，而不破坏历史复现证据。
- 失败现象：盘点发现资产本身已有基本分层；主要问题是历史manifest、新本地入口和外部源码路径混在同一导航中，且adapter尚无明确代码归属。
- 原因判断：直接搬动约25 GiB资产会让既有绝对路径失效，收益低于兼容别名方案。
- 修改内容：新增checkpoint分类软链接、`configs/assets/local_assets.yaml`、`src/soccerfactory_adapters/`和目录说明；历史Gate路径及大型物理文件保持不动。
- 结果变化：新实验已有统一的代码、checkpoint和固定数据命名入口；没有复制大文件、运行GPU或改变Gate结论。
- 是否保留：保留。后续入口逐个迁移，不批量重写历史manifest。

## 2026-08-20｜项目目录收敛

- 假设：把冻结基线、可修改研究代码、大型资产、运行结果和历史证据分成五个一级目录，可以降低日常理解成本，同时不删除复现证据。
- 失败现象：首次批量移动在缺少`research/tests/legacy/`目标目录时停止；首次CPU导入又暴露`sn_calibration`依赖其内部顶层`SoccerPitch`导入路径。
- 原因判断：前者是迁移脚本漏建一层目录；后者是原项目依赖未打包的外部标定源码和工作目录`PYTHONPATH`，不是模型代码或checkpoint损坏。
- 修改内容：建立`baseline/research/assets/runs/archive`；原版代码和官方checkpoint冻结，当前源码改为`soccermaster.*`包导入；历史Gate、实验、配置、工具和结果整体归档；根目录保留薄`train.py/eval.py`入口，并显式加入标定依赖路径。
- 结果变化：核心模型、数据、adapter和配置CPU导入通过；根训练、评估入口的`--help`均退出0；未运行GPU或模型forward。
- 是否保留：保留新结构。旧路径仅用于历史映射和隐藏兼容，不再作为新实验入口。

## 2026-08-20｜目录迁移后的 SoccerMaster 五头路径 smoke

- 假设：整理后的研究源码、配置、官方checkpoint和固定数据视图仍能完成真实五头forward。
- 失败现象：历史五头脚本仍引用迁移前的`models/configs/reports`和已归档实验模块，不能直接作为新入口；正式模型运行无失败。
- 原因判断：阻塞来自旧入口路径，不是模型、checkpoint或数据损坏。
- 修改内容：在`research/reproduction/smokes/`建立当前入口，固定新路径、独立输出、600秒timeout和30秒heartbeat；不改变样本、模型、dtype或任务头。
- 结果变化：经授权在GPU7运行一次，退出码0；输入`[1,30,3,512,512]`，五头与5张图齐全，脚本内77.969秒，峰值allocated/reserved为6,422,995,456/8,529,117,184 bytes。
- 是否保留：保留新入口和`runs/path_smoke_20260819/soccermaster/`结果；只证明迁移后的固定推理路径可运行，不外推训练或总体质量。

## 2026-08-20｜目录迁移后的 SoccerFactory 全链路径 smoke

- 假设：冻结SoccerFactory源码、规范checkpoint/数据路径和研究侧adapter可以在不依赖旧根目录入口的情况下完成固定全链。
- 失败现象：历史launcher仍引用迁移前的`vendor/.local_assets/reports/reproduction`；正式全链运行无失败。
- 原因判断：历史入口路径与新目录不一致，需要新的隔离smoke配置；模型和资产本身可用。
- 修改内容：在`research/reproduction/smokes/soccerfactory/`集中建立当前配置、launcher和manifest；实际算法仍来自冻结`baseline/code/soccerfactory`，输出统一进入`runs/`和`.runtime/`。
- 结果变化：GPU7上Step 1、enrichment、Refiner及CPU Step 3、转换、DataLoader全部退出0；255帧、3,390人物、48轨迹连续保持，最终batch为`[1,30,3,512,512]`，最高reserved显存19,639,828,480 bytes。
- 是否保留：保留新入口和结果；只确认迁移后路径及接口闭环，不把角色、球队、号码、轨迹或坐标字段解释为准确。
